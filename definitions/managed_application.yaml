name: ManagedApplication
module_name: azure_rm_managedapplication
description: "Manage Azure Managed Application instances."
api_version: "2021-07-01"
provider: Microsoft.Solutions
resource_type: applications
generate_info: true
author: "Azure Magic Modules Generator"
doc_url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/managed-applications/overview"
properties:
  name:
    type: str
    required: true
    description: "Name of the Managed Application."
  resource_group:
    type: str
    required: true
    description: "Name of the resource group."
  location:
    type: str
    required: true
    description: "Azure region."
    api_field: location
    location: body
  kind:
    type: str
    description: "Kind of managed application."
    choices:
      - MarketPlace
      - ServiceCatalog
    api_field: kind
    location: body
  managed_resource_group_id:
    type: str
    description: "Resource ID of the managed resource group."
    api_field: managedResourceGroupId
    location: body
  application_definition_id:
    type: str
    description: "Resource ID of the application definition."
    api_field: applicationDefinitionId
    location: body
  parameters:
    type: dict
    description: "Application parameters."
    api_field: parameters
    location: body
  plan_name:
    type: str
    description: "Name of the plan."
    api_field: plan.name
    location: body
  plan_publisher:
    type: str
    description: "Publisher of the plan."
    api_field: plan.publisher
    location: body
  plan_product:
    type: str
    description: "Product of the plan."
    api_field: plan.product
    location: body
  plan_version:
    type: str
    description: "Version of the plan."
    api_field: plan.version
    location: body
  tags:
    type: dict
    description: "Resource tags."
    location: tags
