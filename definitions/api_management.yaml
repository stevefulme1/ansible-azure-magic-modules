name: ApiManagement
module_name: azure_rm_apimanagement
description: "Manage Azure API Management service instances."
api_version: "2023-09-01-preview"
provider: Microsoft.ApiManagement
resource_type: service
generate_info: true
author: "Azure Magic Modules Generator"
doc_url: "https://learn.microsoft.com/en-us/azure/api-management/api-management-key-concepts"
properties:
  name:
    type: str
    required: true
    description: "Name of the API Management service."
  resource_group:
    type: str
    required: true
    description: "Name of the resource group."
  location:
    type: str
    required: true
    description: "Azure region."
    api_field: location
    location: body
  sku_name:
    type: str
    description: "SKU name of the API Management service."
    api_field: sku.name
    location: body
    choices:
      - Developer
      - Basic
      - Standard
      - Premium
      - Consumption
      - Isolated
      - BasicV2
      - StandardV2
  sku_capacity:
    type: int
    description: "SKU capacity of the API Management service."
    api_field: sku.capacity
    location: body
  publisher_name:
    type: str
    description: "Publisher name for the API Management service."
    api_field: publisherName
    location: body
  publisher_email:
    type: str
    description: "Publisher email for the API Management service."
    api_field: publisherEmail
    location: body
  virtual_network_type:
    type: str
    description: "Virtual network type for the service."
    api_field: virtualNetworkType
    location: body
    choices:
      - None
      - External
      - Internal
  identity_type:
    type: str
    description: "Managed identity type for the service."
    api_field: identity.type
    location: body
    choices:
      - SystemAssigned
      - UserAssigned
      - None
  tags:
    type: dict
    description: "Resource tags."
    location: tags
