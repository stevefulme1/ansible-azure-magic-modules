name: IpGroup
module_name: azure_rm_ipgroup
description: "Manage Azure IP groups."
api_version: "2024-03-01"
provider: Microsoft.Network
resource_type: ipGroups
generate_info: true
author: "Azure Magic Modules Generator"
doc_url: "https://learn.microsoft.com/en-us/azure/firewall/ip-groups"
properties:
  name:
    type: str
    required: true
    description: "Name of the IP group."
  resource_group:
    type: str
    required: true
    description: "Name of the resource group."
  location:
    type: str
    required: true
    description: "Azure region."
    api_field: location
    location: body
  ip_addresses:
    type: list
    description: "List of IP addresses or CIDR ranges in the group."
    api_field: ipAddresses
    location: body
    element:
      type: str
  tags:
    type: dict
    description: "Resource tags."
    location: tags
