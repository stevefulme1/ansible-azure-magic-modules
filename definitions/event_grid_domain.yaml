name: EventGridDomain
module_name: azure_rm_eventgriddomain
description: "Manage Azure Event Grid Domain instances."
api_version: "2024-06-01-preview"
provider: Microsoft.EventGrid
resource_type: domains
generate_info: true
author: "Azure Magic Modules Generator"
doc_url: "https://learn.microsoft.com/en-us/azure/event-grid/event-domains"
properties:
  name:
    type: str
    required: true
    description: "Name of the Event Grid Domain."
  resource_group:
    type: str
    required: true
    description: "Name of the resource group."
  location:
    type: str
    required: true
    description: "Azure region."
    api_field: location
    location: body
  input_schema:
    type: str
    description: "Input schema for the Event Grid Domain."
    api_field: inputSchema
    location: body
    choices:
      - EventGridSchema
      - CustomEventSchema
      - CloudEventSchemaV1_0
  public_network_access:
    type: str
    description: "Whether public network access is allowed."
    api_field: publicNetworkAccess
    location: body
    choices:
      - Enabled
      - Disabled
  auto_create_topic_with_first_subscription:
    type: bool
    description: "Whether to auto-create topics with first subscription."
    api_field: autoCreateTopicWithFirstSubscription
    location: body
    default: true
  tags:
    type: dict
    description: "Resource tags."
    location: tags
