name: SharedImageGallery
module_name: azure_rm_sharedimagegallery
description: "Manage Azure Shared Image Gallery instances."
api_version: "2023-07-03"
provider: Microsoft.Compute
resource_type: galleries
generate_info: true
author: "Azure Magic Modules Generator"
doc_url: "https://learn.microsoft.com/en-us/azure/virtual-machines/shared-image-galleries"
properties:
  name:
    type: str
    required: true
    description: "Name of the Shared Image Gallery."
  resource_group:
    type: str
    required: true
    description: "Name of the resource group."
  location:
    type: str
    required: true
    description: "Azure region."
    api_field: location
    location: body
  description:
    type: str
    description: "Description of the gallery."
    api_field: description
    location: body
  sharing_profile_permission:
    type: str
    description: "Sharing permission for the gallery."
    choices:
      - Private
      - Groups
      - Community
    api_field: sharingProfile.permissions
    location: body
  tags:
    type: dict
    description: "Resource tags."
    location: tags
