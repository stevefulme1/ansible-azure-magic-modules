name: EventGridTopic
module_name: azure_rm_eventgridtopic
description: "Manage Azure Event Grid Topic instances."
api_version: "2024-06-01-preview"
provider: Microsoft.EventGrid
resource_type: topics
generate_info: true
author: "Azure Magic Modules Generator"
doc_url: "https://learn.microsoft.com/en-us/azure/event-grid/custom-topics"
properties:
  name:
    type: str
    required: true
    description: "Name of the Event Grid Topic."
  resource_group:
    type: str
    required: true
    description: "Name of the resource group."
  location:
    type: str
    required: true
    description: "Azure region."
    api_field: location
    location: body
  input_schema:
    type: str
    description: "Input schema for the Event Grid Topic."
    api_field: inputSchema
    location: body
    choices:
      - EventGridSchema
      - CustomEventSchema
      - CloudEventSchemaV1_0
  public_network_access:
    type: str
    description: "Whether public network access is allowed."
    api_field: publicNetworkAccess
    location: body
    choices:
      - Enabled
      - Disabled
  identity_type:
    type: str
    description: "Managed identity type for the topic."
    api_field: identity.type
    location: body
  tags:
    type: dict
    description: "Resource tags."
    location: tags
