name: BatchAccount
module_name: azure_rm_batchaccount
description: "Manage Azure Batch account instances."
api_version: "2024-02-01"
provider: Microsoft.Batch
resource_type: batchAccounts
generate_info: true
author: "Azure Magic Modules Generator"
doc_url: "https://learn.microsoft.com/en-us/azure/batch/"
properties:
  name:
    type: str
    required: true
    description: "Name of the Batch account."
  resource_group:
    type: str
    required: true
    description: "Name of the resource group."
  location:
    type: str
    required: true
    description: "Azure region."
    api_field: location
    location: body
  pool_allocation_mode:
    type: str
    description: "Pool allocation mode for the Batch account."
    api_field: poolAllocationMode
    location: body
    choices:
      - BatchService
      - UserSubscription
  auto_storage_account_id:
    type: str
    description: "Resource ID of the storage account for auto-storage."
    api_field: autoStorage.storageAccountId
    location: body
  public_network_access:
    type: str
    description: "Whether public network access is allowed."
    api_field: publicNetworkAccess
    location: body
    choices:
      - Enabled
      - Disabled
  encryption_key_source:
    type: str
    description: "Key source for encryption of the Batch account."
    api_field: encryption.keySource
    location: body
    choices:
      - Microsoft.Batch
      - Microsoft.KeyVault
  tags:
    type: dict
    description: "Resource tags."
    location: tags
