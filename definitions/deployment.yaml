name: Deployment
module_name: azure_rm_deployment
description: "Manage Azure Resource Group Deployments."
api_version: "2022-09-01"
provider: Microsoft.Resources
resource_type: deployments
generate_info: true
author: "Azure Magic Modules Generator"
doc_url: "https://learn.microsoft.com/en-us/azure/azure-resource-manager/templates/deploy-rest"
properties:
  name:
    type: str
    required: true
    description: "Name of the deployment."
  resource_group:
    type: str
    required: true
    description: "Name of the resource group."
  template_content:
    type: dict
    description: "The template content for the deployment."
    api_field: template
    location: body
  template_link_uri:
    type: str
    description: "URI of the linked template."
    api_field: templateLink.uri
    location: body
  parameters:
    type: dict
    description: "Deployment parameters."
    api_field: parameters
    location: body
  parameters_link_uri:
    type: str
    description: "URI of the linked parameters file."
    api_field: parametersLink.uri
    location: body
  mode:
    type: str
    description: "Deployment mode."
    choices:
      - Incremental
      - Complete
    api_field: mode
    location: body
  debug_setting_detail_level:
    type: str
    description: "Detail level for debug settings."
    choices:
      - RequestContent
      - ResponseContent
      - "RequestContent,ResponseContent"
      - None
    api_field: debugSetting.detailLevel
    location: body
