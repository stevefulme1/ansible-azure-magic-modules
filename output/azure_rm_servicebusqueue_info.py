#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_servicebusqueue_info
version_added: "0.1.0"
short_description: Get facts for Azure ServiceBusQueue
description:
    - Get facts for a specific ServiceBusQueue or list all ServiceBusQueues in a resource group.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the servicebusqueue.
            - If not specified, all servicebusqueues in the resource group will be listed.
        type: str
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure ServiceBusQueue REST API
      link: https://learn.microsoft.com/en-us/azure/service-bus-messaging/service-bus-queues-topics-subscriptions
'''

EXAMPLES = r'''
- name: Get facts for a specific ServiceBusQueue
  azure_rm_servicebusqueue_info:
    resource_group: myResourceGroup
    name: myServiceBusQueue

- name: List all ServiceBusQueues in a resource group
  azure_rm_servicebusqueue_info:
    resource_group: myResourceGroup
'''

RETURN = r'''
servicebusqueues:
    description:
        - List of servicebusqueue dicts.
    returned: always
    type: list
    elements: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        namespace_name:
            description: Name of the Service Bus Namespace.
            type: str
        max_size_in_megabytes:
            description: Maximum size of the queue in megabytes.
            type: int
        default_message_ttl:
            description: Default message time-to-live in ISO 8601 format.
            type: str
        lock_duration:
            description: Lock duration for peek-lock in ISO 8601 format.
            type: str
        dead_lettering_on_message_expiration:
            description: Whether to dead-letter messages on expiration.
            type: bool
        requires_duplicate_detection:
            description: Whether duplicate detection is required.
            type: bool
        requires_session:
            description: Whether sessions are required.
            type: bool
        max_delivery_count:
            description: Maximum delivery count before dead-lettering.
            type: int
        enable_partitioning:
            description: Whether partitioning is enabled.
            type: bool
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMServiceBusQueueInfo(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str'),
        )

        self.resource_group = None
        self.name = None
        self.tags = None

        self.results = dict(
            changed=False,
            servicebusqueues=[],
        )

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=False,
            facts_module=True,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        result_key = 'servicebusqueues'

        if self.name:
            response = self.get_resource()
            if response:
                if self.has_tags(response.get('tags'), self.tags):
                    self.results[result_key] = [self.format_response(response)]
        else:
            responses = self.list_by_resource_group()
            for item in responses:
                if self.has_tags(item.get('tags'), self.tags):
                    self.results[result_key].append(self.format_response(item))

        return self.results

    def get_resource(self):
        try:
            url = (
                '/subscriptions/{subscription_id}'
                '/resourceGroups/{resource_group}'
                '/providers/Microsoft.ServiceBus/namespaces/queues/{name}'
            ).format(
                subscription_id=self.subscription_id,
                resource_group=self.resource_group,
                name=self.name,
            )
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2022-10-01-preview'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def list_by_resource_group(self):
        try:
            url = (
                '/subscriptions/{subscription_id}'
                '/resourceGroups/{resource_group}'
                '/providers/Microsoft.ServiceBus/namespaces/queues'
            ).format(
                subscription_id=self.subscription_id,
                resource_group=self.resource_group,
            )
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2022-10-01-preview'},
                None, None, [200], 0, 0,
            )
            result = self.deserialize_response(response)
            if result and isinstance(result, dict):
                return result.get('value', [])
            return []
        except Exception:
            return []

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['namespace_name'] = response.get('NamespaceName')
        result['max_size_in_megabytes'] = response.get('maxSizeInMegabytes')
        result['default_message_ttl'] = response.get('defaultMessageTimeToLive')
        result['lock_duration'] = response.get('lockDuration')
        result['dead_lettering_on_message_expiration'] = response.get('deadLetteringOnMessageExpiration')
        result['requires_duplicate_detection'] = response.get('requiresDuplicateDetection')
        result['requires_session'] = response.get('requiresSession')
        result['max_delivery_count'] = response.get('maxDeliveryCount')
        result['enable_partitioning'] = response.get('enablePartitioning')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMServiceBusQueueInfo()


if __name__ == '__main__':
    main()
