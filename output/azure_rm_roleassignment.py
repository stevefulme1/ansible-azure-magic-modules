#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_roleassignment
version_added: "0.1.0"
short_description: Manage Azure role assignments.
description:
    - Create, update, or delete an Azure RoleAssignment.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the roleassignment.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    scope:
        description:
            - The scope for the role assignment.
        type: str
        required: true
    role_definition_id:
        description:
            - The fully qualified role definition ID.
        type: str
        required: true
    principal_id:
        description:
            - The principal ID of the assignee.
        type: str
        required: true
    principal_type:
        description:
            - The type of the principal.
        type: str
        choices:
            - "User"
            - "Group"
            - "ServicePrincipal"
            - "ForeignGroup"
            - "Device"
    condition:
        description:
            - Conditions for the role assignment.
        type: str
    condition_version:
        description:
            - Version of the condition syntax.
        type: str
        choices:
            - "2.0"
    state:
        description:
            - Assert the state of the roleassignment.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure RoleAssignment REST API
      link: https://learn.microsoft.com/en-us/azure/role-based-access-control/
'''

EXAMPLES = r'''
- name: Create RoleAssignment
  azure_rm_roleassignment:
    resource_group: myResourceGroup
    name: myRoleAssignment
    location: eastus
    scope: "example_value"
    role_definition_id: "example_value"
    principal_id: "example_value"
    state: present

- name: Delete RoleAssignment
  azure_rm_roleassignment:
    resource_group: myResourceGroup
    name: myRoleAssignment
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the roleassignment.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        scope:
            description: The scope for the role assignment.
            type: str
        role_definition_id:
            description: The fully qualified role definition ID.
            type: str
        principal_id:
            description: The principal ID of the assignee.
            type: str
        principal_type:
            description: The type of the principal.
            type: str
        condition:
            description: Conditions for the role assignment.
            type: str
        condition_version:
            description: Version of the condition syntax.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMRoleAssignment(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            scope=dict(
                type='str',
                required=True,
            ),
            role_definition_id=dict(
                type='str',
                required=True,
            ),
            principal_id=dict(
                type='str',
                required=True,
            ),
            principal_type=dict(
                type='str',
                choices=['User', 'Group', 'ServicePrincipal', 'ForeignGroup', 'Device'],
            ),
            condition=dict(
                type='str',
            ),
            condition_version=dict(
                type='str',
                choices=['2.0'],
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.scope = None
        self.role_definition_id = None
        self.principal_id = None
        self.principal_type = None
        self.condition = None
        self.condition_version = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.scope is not None:
            body['scope'] = self.scope

        properties = body.setdefault('properties', {})
        if self.role_definition_id is not None:
            properties['roleDefinitionId'] = self.role_definition_id
        if self.principal_id is not None:
            properties['principalId'] = self.principal_id
        if self.principal_type is not None:
            properties['principalType'] = self.principal_type
        if self.condition is not None:
            properties['condition'] = self.condition
        if self.condition_version is not None:
            properties['conditionVersion'] = self.condition_version

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check scope
        if self.scope is not None:
            if existing.get('scope') != self.scope:
                return True
        # Check role_definition_id
        if self.role_definition_id is not None:
            if existing.get('properties', {}).get('roleDefinitionId') != self.role_definition_id:
                return True
        # Check principal_id
        if self.principal_id is not None:
            if existing.get('properties', {}).get('principalId') != self.principal_id:
                return True
        # Check principal_type
        if self.principal_type is not None:
            if existing.get('properties', {}).get('principalType') != self.principal_type:
                return True
        # Check condition
        if self.condition is not None:
            if existing.get('properties', {}).get('condition') != self.condition:
                return True
        # Check condition_version
        if self.condition_version is not None:
            if existing.get('properties', {}).get('conditionVersion') != self.condition_version:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2022-04-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2022-04-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2022-04-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Authorization/roleAssignments/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['scope'] = response.get('scope')
        result['role_definition_id'] = response.get('properties', {}).get('roleDefinitionId')
        result['principal_id'] = response.get('properties', {}).get('principalId')
        result['principal_type'] = response.get('properties', {}).get('principalType')
        result['condition'] = response.get('properties', {}).get('condition')
        result['condition_version'] = response.get('properties', {}).get('conditionVersion')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMRoleAssignment()


if __name__ == '__main__':
    main()
