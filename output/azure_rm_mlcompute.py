#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_mlcompute
version_added: "0.1.0"
short_description: Manage Azure Machine Learning compute resources.
description:
    - Create, update, or delete an Azure MachineLearningCompute.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the machinelearningcompute.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    workspace_name:
        description:
            - Name of the Machine Learning workspace.
        type: str
        required: true
    compute_type:
        description:
            - Type of compute resource.
        type: str
        required: true
        choices:
            - "AmlCompute"
            - "ComputeInstance"
            - "VirtualMachine"
            - "Databricks"
            - "HDInsight"
            - "Kubernetes"
    vm_size:
        description:
            - Size of the virtual machine.
        type: str
    min_node_count:
        description:
            - Minimum number of nodes for autoscaling.
        type: int
    max_node_count:
        description:
            - Maximum number of nodes for autoscaling.
        type: int
    idle_seconds_before_scaledown:
        description:
            - Idle time in seconds before scaling down.
        type: int
    admin_username:
        description:
            - Admin username for the compute resource.
        type: str
    admin_user_ssh_public_key:
        description:
            - SSH public key for admin user authentication.
        type: str
    state:
        description:
            - Assert the state of the machinelearningcompute.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure MachineLearningCompute REST API
      link: https://learn.microsoft.com/en-us/azure/machine-learning/concept-compute-target
'''

EXAMPLES = r'''
- name: Create MachineLearningCompute
  azure_rm_mlcompute:
    resource_group: myResourceGroup
    name: myMachineLearningCompute
    location: eastus
    workspace_name: "example_value"
    compute_type: "example_value"
    state: present

- name: Delete MachineLearningCompute
  azure_rm_mlcompute:
    resource_group: myResourceGroup
    name: myMachineLearningCompute
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the machinelearningcompute.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        workspace_name:
            description: Name of the Machine Learning workspace.
            type: str
        compute_type:
            description: Type of compute resource.
            type: str
        vm_size:
            description: Size of the virtual machine.
            type: str
        min_node_count:
            description: Minimum number of nodes for autoscaling.
            type: int
        max_node_count:
            description: Maximum number of nodes for autoscaling.
            type: int
        idle_seconds_before_scaledown:
            description: Idle time in seconds before scaling down.
            type: int
        admin_username:
            description: Admin username for the compute resource.
            type: str
        admin_user_ssh_public_key:
            description: SSH public key for admin user authentication.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMMachineLearningCompute(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            workspace_name=dict(
                type='str',
                required=True,
            ),
            compute_type=dict(
                type='str',
                required=True,
                choices=['AmlCompute', 'ComputeInstance', 'VirtualMachine', 'Databricks', 'HDInsight', 'Kubernetes'],
            ),
            vm_size=dict(
                type='str',
            ),
            min_node_count=dict(
                type='int',
            ),
            max_node_count=dict(
                type='int',
            ),
            idle_seconds_before_scaledown=dict(
                type='int',
            ),
            admin_username=dict(
                type='str',
            ),
            admin_user_ssh_public_key=dict(
                type='str',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.workspace_name = None
        self.compute_type = None
        self.vm_size = None
        self.min_node_count = None
        self.max_node_count = None
        self.idle_seconds_before_scaledown = None
        self.admin_username = None
        self.admin_user_ssh_public_key = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.workspace_name is not None:
            body['WorkspaceName'] = self.workspace_name

        properties = body.setdefault('properties', {})
        if self.compute_type is not None:
            properties['computeType'] = self.compute_type
        if self.vm_size is not None:
            _target = properties
            _target = _target.setdefault('properties', {})
            _target['vmSize'] = self.vm_size
        if self.min_node_count is not None:
            _target = properties
            _target = _target.setdefault('properties', {})
            _target = _target.setdefault('scaleSettings', {})
            _target['minNodeCount'] = self.min_node_count
        if self.max_node_count is not None:
            _target = properties
            _target = _target.setdefault('properties', {})
            _target = _target.setdefault('scaleSettings', {})
            _target['maxNodeCount'] = self.max_node_count
        if self.idle_seconds_before_scaledown is not None:
            _target = properties
            _target = _target.setdefault('properties', {})
            _target = _target.setdefault('scaleSettings', {})
            _target['nodeIdleTimeBeforeScaleDown'] = self.idle_seconds_before_scaledown
        if self.admin_username is not None:
            _target = properties
            _target = _target.setdefault('properties', {})
            _target['adminUserName'] = self.admin_username
        if self.admin_user_ssh_public_key is not None:
            _target = properties
            _target = _target.setdefault('properties', {})
            _target['adminUserSshPublicKey'] = self.admin_user_ssh_public_key

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check workspace_name
        if self.workspace_name is not None:
            if existing.get('WorkspaceName') != self.workspace_name:
                return True
        # Check compute_type
        if self.compute_type is not None:
            if existing.get('properties', {}).get('computeType') != self.compute_type:
                return True
        # Check vm_size
        if self.vm_size is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('properties', {}) if isinstance(_existing, dict) else {}
            if _existing.get('vmSize') != self.vm_size:
                return True
        # Check min_node_count
        if self.min_node_count is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('properties', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('scaleSettings', {}) if isinstance(_existing, dict) else {}
            if _existing.get('minNodeCount') != self.min_node_count:
                return True
        # Check max_node_count
        if self.max_node_count is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('properties', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('scaleSettings', {}) if isinstance(_existing, dict) else {}
            if _existing.get('maxNodeCount') != self.max_node_count:
                return True
        # Check idle_seconds_before_scaledown
        if self.idle_seconds_before_scaledown is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('properties', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('scaleSettings', {}) if isinstance(_existing, dict) else {}
            if _existing.get('nodeIdleTimeBeforeScaleDown') != self.idle_seconds_before_scaledown:
                return True
        # Check admin_username
        if self.admin_username is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('properties', {}) if isinstance(_existing, dict) else {}
            if _existing.get('adminUserName') != self.admin_username:
                return True
        # Check admin_user_ssh_public_key
        if self.admin_user_ssh_public_key is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('properties', {}) if isinstance(_existing, dict) else {}
            if _existing.get('adminUserSshPublicKey') != self.admin_user_ssh_public_key:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2023-10-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2023-10-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2023-10-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.MachineLearningServices/workspaces/computes/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['workspace_name'] = response.get('WorkspaceName')
        result['compute_type'] = response.get('properties', {}).get('computeType')
        _props = response.get('properties', {})
        _props = _props.get('properties', {}) if isinstance(_props, dict) else {}
        result['vm_size'] = _props.get('vmSize')
        _props = response.get('properties', {})
        _props = _props.get('properties', {}) if isinstance(_props, dict) else {}
        _props = _props.get('scaleSettings', {}) if isinstance(_props, dict) else {}
        result['min_node_count'] = _props.get('minNodeCount')
        _props = response.get('properties', {})
        _props = _props.get('properties', {}) if isinstance(_props, dict) else {}
        _props = _props.get('scaleSettings', {}) if isinstance(_props, dict) else {}
        result['max_node_count'] = _props.get('maxNodeCount')
        _props = response.get('properties', {})
        _props = _props.get('properties', {}) if isinstance(_props, dict) else {}
        _props = _props.get('scaleSettings', {}) if isinstance(_props, dict) else {}
        result['idle_seconds_before_scaledown'] = _props.get('nodeIdleTimeBeforeScaleDown')
        _props = response.get('properties', {})
        _props = _props.get('properties', {}) if isinstance(_props, dict) else {}
        result['admin_username'] = _props.get('adminUserName')
        _props = response.get('properties', {})
        _props = _props.get('properties', {}) if isinstance(_props, dict) else {}
        result['admin_user_ssh_public_key'] = _props.get('adminUserSshPublicKey')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMMachineLearningCompute()


if __name__ == '__main__':
    main()
