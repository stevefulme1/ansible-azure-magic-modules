#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_kustocluster
version_added: "0.1.0"
short_description: Manage Azure Data Explorer (Kusto) clusters.
description:
    - Create, update, or delete an Azure KustoCluster.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the kustocluster.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    sku_name:
        description:
            - SKU name for the Kusto cluster.
        type: str
        choices:
            - "Dev(No SLA)_Standard_D11_v2"
            - "Standard_D11_v2"
            - "Standard_D12_v2"
            - "Standard_D13_v2"
            - "Standard_D14_v2"
            - "Standard_DS13_v2+1TB_PS"
            - "Standard_DS13_v2+2TB_PS"
            - "Standard_DS14_v2+3TB_PS"
            - "Standard_DS14_v2+4TB_PS"
            - "Standard_L4s"
            - "Standard_L8s"
            - "Standard_L16s"
            - "Standard_L8s_v2"
            - "Standard_L16s_v2"
            - "Standard_L8as_v3"
            - "Standard_L16as_v3"
    sku_tier:
        description:
            - SKU tier for the Kusto cluster.
        type: str
        choices:
            - "Basic"
            - "Standard"
    sku_capacity:
        description:
            - Number of instances in the Kusto cluster.
        type: int
    enable_streaming_ingest:
        description:
            - Whether streaming ingestion is enabled.
        type: bool
    enable_purge:
        description:
            - Whether data purge is enabled.
        type: bool
    enable_double_encryption:
        description:
            - Whether double encryption is enabled.
        type: bool
    engine_type:
        description:
            - Engine type for the Kusto cluster.
        type: str
        choices:
            - "V2"
            - "V3"
    identity_type:
        description:
            - Type of managed identity.
        type: str
        choices:
            - "SystemAssigned"
            - "UserAssigned"
    state:
        description:
            - Assert the state of the kustocluster.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure KustoCluster REST API
      link: https://learn.microsoft.com/en-us/azure/data-explorer/
'''

EXAMPLES = r'''
- name: Create KustoCluster
  azure_rm_kustocluster:
    resource_group: myResourceGroup
    name: myKustoCluster
    location: eastus
    state: present

- name: Delete KustoCluster
  azure_rm_kustocluster:
    resource_group: myResourceGroup
    name: myKustoCluster
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the kustocluster.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        sku_name:
            description: SKU name for the Kusto cluster.
            type: str
        sku_tier:
            description: SKU tier for the Kusto cluster.
            type: str
        sku_capacity:
            description: Number of instances in the Kusto cluster.
            type: int
        enable_streaming_ingest:
            description: Whether streaming ingestion is enabled.
            type: bool
        enable_purge:
            description: Whether data purge is enabled.
            type: bool
        enable_double_encryption:
            description: Whether double encryption is enabled.
            type: bool
        engine_type:
            description: Engine type for the Kusto cluster.
            type: str
        identity_type:
            description: Type of managed identity.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMKustoCluster(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            sku_name=dict(
                type='str',
                choices=[
                    'Dev(No SLA)_Standard_D11_v2',
                    'Standard_D11_v2',
                    'Standard_D12_v2',
                    'Standard_D13_v2',
                    'Standard_D14_v2',
                    'Standard_DS13_v2+1TB_PS',
                    'Standard_DS13_v2+2TB_PS',
                    'Standard_DS14_v2+3TB_PS',
                    'Standard_DS14_v2+4TB_PS',
                    'Standard_L4s',
                    'Standard_L8s',
                    'Standard_L16s',
                    'Standard_L8s_v2',
                    'Standard_L16s_v2',
                    'Standard_L8as_v3',
                    'Standard_L16as_v3',
                ],
            ),
            sku_tier=dict(
                type='str',
                choices=['Basic', 'Standard'],
            ),
            sku_capacity=dict(
                type='int',
            ),
            enable_streaming_ingest=dict(
                type='bool',
            ),
            enable_purge=dict(
                type='bool',
            ),
            enable_double_encryption=dict(
                type='bool',
            ),
            engine_type=dict(
                type='str',
                choices=['V2', 'V3'],
            ),
            identity_type=dict(
                type='str',
                choices=['SystemAssigned', 'UserAssigned'],
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.sku_name = None
        self.sku_tier = None
        self.sku_capacity = None
        self.enable_streaming_ingest = None
        self.enable_purge = None
        self.enable_double_encryption = None
        self.engine_type = None
        self.identity_type = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.sku_name is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['name'] = self.sku_name
        if self.sku_tier is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['tier'] = self.sku_tier
        if self.sku_capacity is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['capacity'] = self.sku_capacity
        if self.enable_streaming_ingest is not None:
            body['enableStreamingIngest'] = self.enable_streaming_ingest
        if self.enable_purge is not None:
            body['enablePurge'] = self.enable_purge
        if self.enable_double_encryption is not None:
            body['enableDoubleEncryption'] = self.enable_double_encryption
        if self.engine_type is not None:
            body['engineType'] = self.engine_type
        if self.identity_type is not None:
            _target = body
            _target = _target.setdefault('identity', {})
            _target['type'] = self.identity_type

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check sku_name
        if self.sku_name is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('name') != self.sku_name:
                return True
        # Check sku_tier
        if self.sku_tier is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('tier') != self.sku_tier:
                return True
        # Check sku_capacity
        if self.sku_capacity is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('capacity') != self.sku_capacity:
                return True
        # Check enable_streaming_ingest
        if self.enable_streaming_ingest is not None:
            if existing.get('enableStreamingIngest') != self.enable_streaming_ingest:
                return True
        # Check enable_purge
        if self.enable_purge is not None:
            if existing.get('enablePurge') != self.enable_purge:
                return True
        # Check enable_double_encryption
        if self.enable_double_encryption is not None:
            if existing.get('enableDoubleEncryption') != self.enable_double_encryption:
                return True
        # Check engine_type
        if self.engine_type is not None:
            if existing.get('engineType') != self.engine_type:
                return True
        # Check identity_type
        if self.identity_type is not None:
            _existing = existing
            _existing = _existing.get('identity', {}) if isinstance(_existing, dict) else {}
            if _existing.get('type') != self.identity_type:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2023-08-15'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2023-08-15'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2023-08-15'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Kusto/clusters/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_name'] = _body.get('name')
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_tier'] = _body.get('tier')
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_capacity'] = _body.get('capacity')
        result['enable_streaming_ingest'] = response.get('enableStreamingIngest')
        result['enable_purge'] = response.get('enablePurge')
        result['enable_double_encryption'] = response.get('enableDoubleEncryption')
        result['engine_type'] = response.get('engineType')
        _body = response
        _body = _body.get('identity', {}) if isinstance(_body, dict) else {}
        result['identity_type'] = _body.get('type')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMKustoCluster()


if __name__ == '__main__':
    main()
