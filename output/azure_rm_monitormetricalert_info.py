#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_monitormetricalert_info
version_added: "0.1.0"
short_description: Get facts for Azure MonitorMetricAlert
description:
    - Get facts for a specific MonitorMetricAlert or list all MonitorMetricAlerts in a resource group.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the monitormetricalert.
            - If not specified, all monitormetricalerts in the resource group will be listed.
        type: str
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure MonitorMetricAlert REST API
      link: https://learn.microsoft.com/en-us/azure/azure-monitor/alerts/alerts-metric-overview
'''

EXAMPLES = r'''
- name: Get facts for a specific MonitorMetricAlert
  azure_rm_monitormetricalert_info:
    resource_group: myResourceGroup
    name: myMonitorMetricAlert

- name: List all MonitorMetricAlerts in a resource group
  azure_rm_monitormetricalert_info:
    resource_group: myResourceGroup
'''

RETURN = r'''
monitormetricalerts:
    description:
        - List of monitormetricalert dicts.
    returned: always
    type: list
    elements: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        description:
            description: Description of the metric alert.
            type: str
        severity:
            description: Alert severity level.
            type: int
        enabled:
            description: Whether the metric alert is enabled.
            type: bool
        scopes:
            description: List of resource IDs this alert monitors.
            type: list
        evaluation_frequency:
            description: How often the metric alert is evaluated (ISO 8601).
            type: str
        window_size:
            description: Period of time to aggregate metrics (ISO 8601).
            type: str
        target_resource_type:
            description: The resource type of the target resources.
            type: str
        target_resource_region:
            description: The region of the target resources.
            type: str
        auto_mitigate:
            description: Whether the alert should be auto-resolved.
            type: bool
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMMonitorMetricAlertInfo(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str'),
        )

        self.resource_group = None
        self.name = None
        self.tags = None

        self.results = dict(
            changed=False,
            monitormetricalerts=[],
        )

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=False,
            facts_module=True,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        result_key = 'monitormetricalerts'

        if self.name:
            response = self.get_resource()
            if response:
                if self.has_tags(response.get('tags'), self.tags):
                    self.results[result_key] = [self.format_response(response)]
        else:
            responses = self.list_by_resource_group()
            for item in responses:
                if self.has_tags(item.get('tags'), self.tags):
                    self.results[result_key].append(self.format_response(item))

        return self.results

    def get_resource(self):
        try:
            url = (
                '/subscriptions/{subscription_id}'
                '/resourceGroups/{resource_group}'
                '/providers/Microsoft.Insights/metricAlerts/{name}'
            ).format(
                subscription_id=self.subscription_id,
                resource_group=self.resource_group,
                name=self.name,
            )
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2018-03-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def list_by_resource_group(self):
        try:
            url = (
                '/subscriptions/{subscription_id}'
                '/resourceGroups/{resource_group}'
                '/providers/Microsoft.Insights/metricAlerts'
            ).format(
                subscription_id=self.subscription_id,
                resource_group=self.resource_group,
            )
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2018-03-01'},
                None, None, [200], 0, 0,
            )
            result = self.deserialize_response(response)
            if result and isinstance(result, dict):
                return result.get('value', [])
            return []
        except Exception:
            return []

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['description'] = response.get('properties', {}).get('description')
        result['severity'] = response.get('properties', {}).get('severity')
        result['enabled'] = response.get('properties', {}).get('enabled')
        result['scopes'] = response.get('properties', {}).get('scopes')
        result['evaluation_frequency'] = response.get('properties', {}).get('evaluationFrequency')
        result['window_size'] = response.get('properties', {}).get('windowSize')
        result['target_resource_type'] = response.get('properties', {}).get('targetResourceType')
        result['target_resource_region'] = response.get('properties', {}).get('targetResourceRegion')
        result['auto_mitigate'] = response.get('properties', {}).get('autoMitigate')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMMonitorMetricAlertInfo()


if __name__ == '__main__':
    main()
