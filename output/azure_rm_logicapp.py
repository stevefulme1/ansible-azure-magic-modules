#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_logicapp
version_added: "0.1.0"
short_description: Manage Azure Logic App workflow instances.
description:
    - Create, update, or delete an Azure LogicApp.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the logicapp.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    workflow_state:
        description:
            - State of the Logic App workflow.
        type: str
        choices:
            - "Enabled"
            - "Disabled"
    definition:
        description:
            - Workflow definition in JSON format.
        type: dict
    parameters:
        description:
            - Workflow parameters.
        type: dict
    integration_service_environment_id:
        description:
            - Resource ID of the integration service environment.
        type: str
    state:
        description:
            - Assert the state of the logicapp.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure LogicApp REST API
      link: https://learn.microsoft.com/en-us/azure/logic-apps/logic-apps-overview
'''

EXAMPLES = r'''
- name: Create LogicApp
  azure_rm_logicapp:
    resource_group: myResourceGroup
    name: myLogicApp
    location: eastus
    state: present

- name: Delete LogicApp
  azure_rm_logicapp:
    resource_group: myResourceGroup
    name: myLogicApp
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the logicapp.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        workflow_state:
            description: State of the Logic App workflow.
            type: str
        definition:
            description: Workflow definition in JSON format.
            type: dict
        parameters:
            description: Workflow parameters.
            type: dict
        integration_service_environment_id:
            description: Resource ID of the integration service environment.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMLogicApp(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            workflow_state=dict(
                type='str',
                choices=['Enabled', 'Disabled'],
            ),
            definition=dict(
                type='dict',
            ),
            parameters=dict(
                type='dict',
            ),
            integration_service_environment_id=dict(
                type='str',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.workflow_state = None
        self.definition = None
        self.parameters = None
        self.integration_service_environment_id = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.workflow_state is not None:
            body['state'] = self.workflow_state
        if self.definition is not None:
            body['definition'] = self.definition
        if self.parameters is not None:
            body['parameters'] = self.parameters
        if self.integration_service_environment_id is not None:
            _target = body
            _target = _target.setdefault('integrationServiceEnvironment', {})
            _target['id'] = self.integration_service_environment_id

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check workflow_state
        if self.workflow_state is not None:
            if existing.get('state') != self.workflow_state:
                return True
        # Check definition
        if self.definition is not None:
            if existing.get('definition') != self.definition:
                return True
        # Check parameters
        if self.parameters is not None:
            if existing.get('parameters') != self.parameters:
                return True
        # Check integration_service_environment_id
        if self.integration_service_environment_id is not None:
            _existing = existing
            _existing = _existing.get('integrationServiceEnvironment', {}) if isinstance(_existing, dict) else {}
            if _existing.get('id') != self.integration_service_environment_id:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2019-05-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2019-05-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2019-05-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Logic/workflows/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['workflow_state'] = response.get('state')
        result['definition'] = response.get('definition')
        result['parameters'] = response.get('parameters')
        _body = response
        _body = _body.get('integrationServiceEnvironment', {}) if isinstance(_body, dict) else {}
        result['integration_service_environment_id'] = _body.get('id')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMLogicApp()


if __name__ == '__main__':
    main()
