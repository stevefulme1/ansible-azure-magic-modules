#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_hdinsightcluster
version_added: "0.1.0"
short_description: Manage Azure HDInsight clusters.
description:
    - Create, update, or delete an Azure HDInsightCluster.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the hdinsightcluster.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    cluster_version:
        description:
            - HDInsight cluster version.
        type: str
    os_type:
        description:
            - Operating system type for the cluster.
        type: str
        choices:
            - "Linux"
    tier:
        description:
            - Cluster tier.
        type: str
        choices:
            - "Standard"
            - "Premium"
    kind:
        description:
            - Type of HDInsight cluster.
        type: str
        choices:
            - "hadoop"
            - "hbase"
            - "storm"
            - "spark"
            - "interactivehive"
            - "kafka"
            - "rserver"
            - "mlservices"
    gateway_username:
        description:
            - Gateway HTTP username for the cluster.
        type: str
    gateway_password:
        description:
            - Gateway HTTP password for the cluster.
        type: str
    head_node_vm_size:
        description:
            - VM size for the head nodes.
        type: str
    worker_node_vm_size:
        description:
            - VM size for the worker nodes.
        type: str
    worker_node_count:
        description:
            - Number of worker nodes in the cluster.
        type: int
    state:
        description:
            - Assert the state of the hdinsightcluster.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure HDInsightCluster REST API
      link: https://learn.microsoft.com/en-us/azure/hdinsight/
'''

EXAMPLES = r'''
- name: Create HDInsightCluster
  azure_rm_hdinsightcluster:
    resource_group: myResourceGroup
    name: myHDInsightCluster
    location: eastus
    state: present

- name: Delete HDInsightCluster
  azure_rm_hdinsightcluster:
    resource_group: myResourceGroup
    name: myHDInsightCluster
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the hdinsightcluster.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        cluster_version:
            description: HDInsight cluster version.
            type: str
        os_type:
            description: Operating system type for the cluster.
            type: str
        tier:
            description: Cluster tier.
            type: str
        kind:
            description: Type of HDInsight cluster.
            type: str
        gateway_username:
            description: Gateway HTTP username for the cluster.
            type: str
        gateway_password:
            description: Gateway HTTP password for the cluster.
            type: str
        head_node_vm_size:
            description: VM size for the head nodes.
            type: str
        worker_node_vm_size:
            description: VM size for the worker nodes.
            type: str
        worker_node_count:
            description: Number of worker nodes in the cluster.
            type: int
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMHDInsightCluster(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            cluster_version=dict(
                type='str',
            ),
            os_type=dict(
                type='str',
                choices=['Linux'],
            ),
            tier=dict(
                type='str',
                choices=['Standard', 'Premium'],
            ),
            kind=dict(
                type='str',
                choices=['hadoop', 'hbase', 'storm', 'spark', 'interactivehive', 'kafka', 'rserver', 'mlservices'],
            ),
            gateway_username=dict(
                type='str',
            ),
            gateway_password=dict(
                type='str',
            ),
            head_node_vm_size=dict(
                type='str',
            ),
            worker_node_vm_size=dict(
                type='str',
            ),
            worker_node_count=dict(
                type='int',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.cluster_version = None
        self.os_type = None
        self.tier = None
        self.kind = None
        self.gateway_username = None
        self.gateway_password = None
        self.head_node_vm_size = None
        self.worker_node_vm_size = None
        self.worker_node_count = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.cluster_version is not None:
            body['clusterVersion'] = self.cluster_version
        if self.os_type is not None:
            body['osType'] = self.os_type
        if self.tier is not None:
            body['tier'] = self.tier
        if self.kind is not None:
            body['kind'] = self.kind
        if self.gateway_username is not None:
            _target = body
            _target = _target.setdefault('gateway', {})
            _target = _target.setdefault('restAuthCredential', {})
            _target['username'] = self.gateway_username
        if self.gateway_password is not None:
            _target = body
            _target = _target.setdefault('gateway', {})
            _target = _target.setdefault('restAuthCredential', {})
            _target['password'] = self.gateway_password
        if self.head_node_vm_size is not None:
            _target = body
            _target = _target.setdefault('computeProfile', {})
            _target = _target.setdefault('roles', {})
            _target = _target.setdefault('headnode', {})
            _target = _target.setdefault('hardwareProfile', {})
            _target['vmSize'] = self.head_node_vm_size
        if self.worker_node_vm_size is not None:
            _target = body
            _target = _target.setdefault('computeProfile', {})
            _target = _target.setdefault('roles', {})
            _target = _target.setdefault('workernode', {})
            _target = _target.setdefault('hardwareProfile', {})
            _target['vmSize'] = self.worker_node_vm_size
        if self.worker_node_count is not None:
            _target = body
            _target = _target.setdefault('computeProfile', {})
            _target = _target.setdefault('roles', {})
            _target = _target.setdefault('workernode', {})
            _target['targetInstanceCount'] = self.worker_node_count

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check cluster_version
        if self.cluster_version is not None:
            if existing.get('clusterVersion') != self.cluster_version:
                return True
        # Check os_type
        if self.os_type is not None:
            if existing.get('osType') != self.os_type:
                return True
        # Check tier
        if self.tier is not None:
            if existing.get('tier') != self.tier:
                return True
        # Check kind
        if self.kind is not None:
            if existing.get('kind') != self.kind:
                return True
        # Check gateway_username
        if self.gateway_username is not None:
            _existing = existing
            _existing = _existing.get('gateway', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('restAuthCredential', {}) if isinstance(_existing, dict) else {}
            if _existing.get('username') != self.gateway_username:
                return True
        # Check gateway_password
        if self.gateway_password is not None:
            _existing = existing
            _existing = _existing.get('gateway', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('restAuthCredential', {}) if isinstance(_existing, dict) else {}
            if _existing.get('password') != self.gateway_password:
                return True
        # Check head_node_vm_size
        if self.head_node_vm_size is not None:
            _existing = existing
            _existing = _existing.get('computeProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('roles', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('headnode', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('hardwareProfile', {}) if isinstance(_existing, dict) else {}
            if _existing.get('vmSize') != self.head_node_vm_size:
                return True
        # Check worker_node_vm_size
        if self.worker_node_vm_size is not None:
            _existing = existing
            _existing = _existing.get('computeProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('roles', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('workernode', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('hardwareProfile', {}) if isinstance(_existing, dict) else {}
            if _existing.get('vmSize') != self.worker_node_vm_size:
                return True
        # Check worker_node_count
        if self.worker_node_count is not None:
            _existing = existing
            _existing = _existing.get('computeProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('roles', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('workernode', {}) if isinstance(_existing, dict) else {}
            if _existing.get('targetInstanceCount') != self.worker_node_count:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2021-06-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2021-06-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2021-06-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.HDInsight/clusters/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['cluster_version'] = response.get('clusterVersion')
        result['os_type'] = response.get('osType')
        result['tier'] = response.get('tier')
        result['kind'] = response.get('kind')
        _body = response
        _body = _body.get('gateway', {}) if isinstance(_body, dict) else {}
        _body = _body.get('restAuthCredential', {}) if isinstance(_body, dict) else {}
        result['gateway_username'] = _body.get('username')
        _body = response
        _body = _body.get('gateway', {}) if isinstance(_body, dict) else {}
        _body = _body.get('restAuthCredential', {}) if isinstance(_body, dict) else {}
        result['gateway_password'] = _body.get('password')
        _body = response
        _body = _body.get('computeProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('roles', {}) if isinstance(_body, dict) else {}
        _body = _body.get('headnode', {}) if isinstance(_body, dict) else {}
        _body = _body.get('hardwareProfile', {}) if isinstance(_body, dict) else {}
        result['head_node_vm_size'] = _body.get('vmSize')
        _body = response
        _body = _body.get('computeProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('roles', {}) if isinstance(_body, dict) else {}
        _body = _body.get('workernode', {}) if isinstance(_body, dict) else {}
        _body = _body.get('hardwareProfile', {}) if isinstance(_body, dict) else {}
        result['worker_node_vm_size'] = _body.get('vmSize')
        _body = response
        _body = _body.get('computeProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('roles', {}) if isinstance(_body, dict) else {}
        _body = _body.get('workernode', {}) if isinstance(_body, dict) else {}
        result['worker_node_count'] = _body.get('targetInstanceCount')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMHDInsightCluster()


if __name__ == '__main__':
    main()
