#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_synapsesparkpool
version_added: "0.1.0"
short_description: Manage Azure Synapse Analytics Spark pools.
description:
    - Create, update, or delete an Azure SynapseSparkPool.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the synapsesparkpool.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    workspace_name:
        description:
            - Name of the parent Synapse workspace.
        type: str
        required: true
    node_size:
        description:
            - Size of the cluster nodes.
        type: str
        choices:
            - "Small"
            - "Medium"
            - "Large"
            - "XLarge"
            - "XXLarge"
            - "XXXLarge"
    node_size_family:
        description:
            - Node size family for the cluster.
        type: str
        choices:
            - "MemoryOptimized"
            - "HardwareAcceleratedFPGA"
            - "HardwareAcceleratedGPU"
    node_count:
        description:
            - Number of nodes in the Spark pool.
        type: int
    auto_scale_enabled:
        description:
            - Whether auto-scaling is enabled.
        type: bool
    auto_scale_min_node_count:
        description:
            - Minimum number of nodes when auto-scale is enabled.
        type: int
    auto_scale_max_node_count:
        description:
            - Maximum number of nodes when auto-scale is enabled.
        type: int
    auto_pause_enabled:
        description:
            - Whether auto-pause is enabled.
        type: bool
    auto_pause_delay_in_minutes:
        description:
            - Delay in minutes before auto-pause activates.
        type: int
    spark_version:
        description:
            - Apache Spark version for the pool.
        type: str
    state:
        description:
            - Assert the state of the synapsesparkpool.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure SynapseSparkPool REST API
      link: https://learn.microsoft.com/en-us/azure/synapse-analytics/spark/
'''

EXAMPLES = r'''
- name: Create SynapseSparkPool
  azure_rm_synapsesparkpool:
    resource_group: myResourceGroup
    name: mySynapseSparkPool
    location: eastus
    workspace_name: "example_value"
    state: present

- name: Delete SynapseSparkPool
  azure_rm_synapsesparkpool:
    resource_group: myResourceGroup
    name: mySynapseSparkPool
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the synapsesparkpool.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        workspace_name:
            description: Name of the parent Synapse workspace.
            type: str
        node_size:
            description: Size of the cluster nodes.
            type: str
        node_size_family:
            description: Node size family for the cluster.
            type: str
        node_count:
            description: Number of nodes in the Spark pool.
            type: int
        auto_scale_enabled:
            description: Whether auto-scaling is enabled.
            type: bool
        auto_scale_min_node_count:
            description: Minimum number of nodes when auto-scale is enabled.
            type: int
        auto_scale_max_node_count:
            description: Maximum number of nodes when auto-scale is enabled.
            type: int
        auto_pause_enabled:
            description: Whether auto-pause is enabled.
            type: bool
        auto_pause_delay_in_minutes:
            description: Delay in minutes before auto-pause activates.
            type: int
        spark_version:
            description: Apache Spark version for the pool.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMSynapseSparkPool(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            workspace_name=dict(
                type='str',
                required=True,
            ),
            node_size=dict(
                type='str',
                choices=['Small', 'Medium', 'Large', 'XLarge', 'XXLarge', 'XXXLarge'],
            ),
            node_size_family=dict(
                type='str',
                choices=['MemoryOptimized', 'HardwareAcceleratedFPGA', 'HardwareAcceleratedGPU'],
            ),
            node_count=dict(
                type='int',
            ),
            auto_scale_enabled=dict(
                type='bool',
            ),
            auto_scale_min_node_count=dict(
                type='int',
            ),
            auto_scale_max_node_count=dict(
                type='int',
            ),
            auto_pause_enabled=dict(
                type='bool',
            ),
            auto_pause_delay_in_minutes=dict(
                type='int',
            ),
            spark_version=dict(
                type='str',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.workspace_name = None
        self.node_size = None
        self.node_size_family = None
        self.node_count = None
        self.auto_scale_enabled = None
        self.auto_scale_min_node_count = None
        self.auto_scale_max_node_count = None
        self.auto_pause_enabled = None
        self.auto_pause_delay_in_minutes = None
        self.spark_version = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.workspace_name is not None:
            body['WorkspaceName'] = self.workspace_name
        if self.node_size is not None:
            body['nodeSize'] = self.node_size
        if self.node_size_family is not None:
            body['nodeSizeFamily'] = self.node_size_family
        if self.node_count is not None:
            body['nodeCount'] = self.node_count
        if self.auto_scale_enabled is not None:
            _target = body
            _target = _target.setdefault('autoScale', {})
            _target['enabled'] = self.auto_scale_enabled
        if self.auto_scale_min_node_count is not None:
            _target = body
            _target = _target.setdefault('autoScale', {})
            _target['minNodeCount'] = self.auto_scale_min_node_count
        if self.auto_scale_max_node_count is not None:
            _target = body
            _target = _target.setdefault('autoScale', {})
            _target['maxNodeCount'] = self.auto_scale_max_node_count
        if self.auto_pause_enabled is not None:
            _target = body
            _target = _target.setdefault('autoPause', {})
            _target['enabled'] = self.auto_pause_enabled
        if self.auto_pause_delay_in_minutes is not None:
            _target = body
            _target = _target.setdefault('autoPause', {})
            _target['delayInMinutes'] = self.auto_pause_delay_in_minutes
        if self.spark_version is not None:
            body['sparkVersion'] = self.spark_version

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check workspace_name
        if self.workspace_name is not None:
            if existing.get('WorkspaceName') != self.workspace_name:
                return True
        # Check node_size
        if self.node_size is not None:
            if existing.get('nodeSize') != self.node_size:
                return True
        # Check node_size_family
        if self.node_size_family is not None:
            if existing.get('nodeSizeFamily') != self.node_size_family:
                return True
        # Check node_count
        if self.node_count is not None:
            if existing.get('nodeCount') != self.node_count:
                return True
        # Check auto_scale_enabled
        if self.auto_scale_enabled is not None:
            _existing = existing
            _existing = _existing.get('autoScale', {}) if isinstance(_existing, dict) else {}
            if _existing.get('enabled') != self.auto_scale_enabled:
                return True
        # Check auto_scale_min_node_count
        if self.auto_scale_min_node_count is not None:
            _existing = existing
            _existing = _existing.get('autoScale', {}) if isinstance(_existing, dict) else {}
            if _existing.get('minNodeCount') != self.auto_scale_min_node_count:
                return True
        # Check auto_scale_max_node_count
        if self.auto_scale_max_node_count is not None:
            _existing = existing
            _existing = _existing.get('autoScale', {}) if isinstance(_existing, dict) else {}
            if _existing.get('maxNodeCount') != self.auto_scale_max_node_count:
                return True
        # Check auto_pause_enabled
        if self.auto_pause_enabled is not None:
            _existing = existing
            _existing = _existing.get('autoPause', {}) if isinstance(_existing, dict) else {}
            if _existing.get('enabled') != self.auto_pause_enabled:
                return True
        # Check auto_pause_delay_in_minutes
        if self.auto_pause_delay_in_minutes is not None:
            _existing = existing
            _existing = _existing.get('autoPause', {}) if isinstance(_existing, dict) else {}
            if _existing.get('delayInMinutes') != self.auto_pause_delay_in_minutes:
                return True
        # Check spark_version
        if self.spark_version is not None:
            if existing.get('sparkVersion') != self.spark_version:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2021-06-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2021-06-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2021-06-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Synapse/workspaces/bigDataPools/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['workspace_name'] = response.get('WorkspaceName')
        result['node_size'] = response.get('nodeSize')
        result['node_size_family'] = response.get('nodeSizeFamily')
        result['node_count'] = response.get('nodeCount')
        _body = response
        _body = _body.get('autoScale', {}) if isinstance(_body, dict) else {}
        result['auto_scale_enabled'] = _body.get('enabled')
        _body = response
        _body = _body.get('autoScale', {}) if isinstance(_body, dict) else {}
        result['auto_scale_min_node_count'] = _body.get('minNodeCount')
        _body = response
        _body = _body.get('autoScale', {}) if isinstance(_body, dict) else {}
        result['auto_scale_max_node_count'] = _body.get('maxNodeCount')
        _body = response
        _body = _body.get('autoPause', {}) if isinstance(_body, dict) else {}
        result['auto_pause_enabled'] = _body.get('enabled')
        _body = response
        _body = _body.get('autoPause', {}) if isinstance(_body, dict) else {}
        result['auto_pause_delay_in_minutes'] = _body.get('delayInMinutes')
        result['spark_version'] = response.get('sparkVersion')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMSynapseSparkPool()


if __name__ == '__main__':
    main()
