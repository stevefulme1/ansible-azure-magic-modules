#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_containerapp
version_added: "0.1.0"
short_description: Manage Azure Container App instances.
description:
    - Create, update, or delete an Azure ContainerApp.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the containerapp.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    managed_environment_id:
        description:
            - Resource ID of the managed environment.
        type: str
    revision_mode:
        description:
            - Revision mode of the Container App.
        type: str
        choices:
            - "Single"
            - "Multiple"
    ingress_external:
        description:
            - Whether ingress is exposed externally.
        type: bool
    ingress_target_port:
        description:
            - Target port for ingress traffic.
        type: int
    ingress_transport:
        description:
            - Transport protocol for ingress.
        type: str
        choices:
            - "auto"
            - "http"
            - "http2"
            - "tcp"
    container_name:
        description:
            - Name of the container.
        type: str
    container_image:
        description:
            - Container image name and tag.
        type: str
    container_cpu:
        description:
            - CPU cores allocated to the container.
        type: float
    container_memory:
        description:
            - Memory allocated to the container (e.g. 0.5Gi).
        type: str
    min_replicas:
        description:
            - Minimum number of replicas.
        type: int
    max_replicas:
        description:
            - Maximum number of replicas.
        type: int
    state:
        description:
            - Assert the state of the containerapp.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure ContainerApp REST API
      link: https://learn.microsoft.com/en-us/azure/container-apps/overview
'''

EXAMPLES = r'''
- name: Create ContainerApp
  azure_rm_containerapp:
    resource_group: myResourceGroup
    name: myContainerApp
    location: eastus
    state: present

- name: Delete ContainerApp
  azure_rm_containerapp:
    resource_group: myResourceGroup
    name: myContainerApp
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the containerapp.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        managed_environment_id:
            description: Resource ID of the managed environment.
            type: str
        revision_mode:
            description: Revision mode of the Container App.
            type: str
        ingress_external:
            description: Whether ingress is exposed externally.
            type: bool
        ingress_target_port:
            description: Target port for ingress traffic.
            type: int
        ingress_transport:
            description: Transport protocol for ingress.
            type: str
        container_name:
            description: Name of the container.
            type: str
        container_image:
            description: Container image name and tag.
            type: str
        container_cpu:
            description: CPU cores allocated to the container.
            type: float
        container_memory:
            description: Memory allocated to the container (e.g. 0.5Gi).
            type: str
        min_replicas:
            description: Minimum number of replicas.
            type: int
        max_replicas:
            description: Maximum number of replicas.
            type: int
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMContainerApp(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            managed_environment_id=dict(
                type='str',
            ),
            revision_mode=dict(
                type='str',
                choices=['Single', 'Multiple'],
            ),
            ingress_external=dict(
                type='bool',
            ),
            ingress_target_port=dict(
                type='int',
            ),
            ingress_transport=dict(
                type='str',
                choices=['auto', 'http', 'http2', 'tcp'],
            ),
            container_name=dict(
                type='str',
            ),
            container_image=dict(
                type='str',
            ),
            container_cpu=dict(
                type='float',
            ),
            container_memory=dict(
                type='str',
            ),
            min_replicas=dict(
                type='int',
            ),
            max_replicas=dict(
                type='int',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.managed_environment_id = None
        self.revision_mode = None
        self.ingress_external = None
        self.ingress_target_port = None
        self.ingress_transport = None
        self.container_name = None
        self.container_image = None
        self.container_cpu = None
        self.container_memory = None
        self.min_replicas = None
        self.max_replicas = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.managed_environment_id is not None:
            body['managedEnvironmentId'] = self.managed_environment_id
        if self.revision_mode is not None:
            _target = body
            _target = _target.setdefault('configuration', {})
            _target['activeRevisionsMode'] = self.revision_mode
        if self.ingress_external is not None:
            _target = body
            _target = _target.setdefault('configuration', {})
            _target = _target.setdefault('ingress', {})
            _target['external'] = self.ingress_external
        if self.ingress_target_port is not None:
            _target = body
            _target = _target.setdefault('configuration', {})
            _target = _target.setdefault('ingress', {})
            _target['targetPort'] = self.ingress_target_port
        if self.ingress_transport is not None:
            _target = body
            _target = _target.setdefault('configuration', {})
            _target = _target.setdefault('ingress', {})
            _target['transport'] = self.ingress_transport
        if self.container_name is not None:
            _target = body
            _target = _target.setdefault('template', {})
            _target = _target.setdefault('containers[0]', {})
            _target['name'] = self.container_name
        if self.container_image is not None:
            _target = body
            _target = _target.setdefault('template', {})
            _target = _target.setdefault('containers[0]', {})
            _target['image'] = self.container_image
        if self.container_cpu is not None:
            _target = body
            _target = _target.setdefault('template', {})
            _target = _target.setdefault('containers[0]', {})
            _target = _target.setdefault('resources', {})
            _target['cpu'] = self.container_cpu
        if self.container_memory is not None:
            _target = body
            _target = _target.setdefault('template', {})
            _target = _target.setdefault('containers[0]', {})
            _target = _target.setdefault('resources', {})
            _target['memory'] = self.container_memory
        if self.min_replicas is not None:
            _target = body
            _target = _target.setdefault('template', {})
            _target = _target.setdefault('scale', {})
            _target['minReplicas'] = self.min_replicas
        if self.max_replicas is not None:
            _target = body
            _target = _target.setdefault('template', {})
            _target = _target.setdefault('scale', {})
            _target['maxReplicas'] = self.max_replicas

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check managed_environment_id
        if self.managed_environment_id is not None:
            if existing.get('managedEnvironmentId') != self.managed_environment_id:
                return True
        # Check revision_mode
        if self.revision_mode is not None:
            _existing = existing
            _existing = _existing.get('configuration', {}) if isinstance(_existing, dict) else {}
            if _existing.get('activeRevisionsMode') != self.revision_mode:
                return True
        # Check ingress_external
        if self.ingress_external is not None:
            _existing = existing
            _existing = _existing.get('configuration', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('ingress', {}) if isinstance(_existing, dict) else {}
            if _existing.get('external') != self.ingress_external:
                return True
        # Check ingress_target_port
        if self.ingress_target_port is not None:
            _existing = existing
            _existing = _existing.get('configuration', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('ingress', {}) if isinstance(_existing, dict) else {}
            if _existing.get('targetPort') != self.ingress_target_port:
                return True
        # Check ingress_transport
        if self.ingress_transport is not None:
            _existing = existing
            _existing = _existing.get('configuration', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('ingress', {}) if isinstance(_existing, dict) else {}
            if _existing.get('transport') != self.ingress_transport:
                return True
        # Check container_name
        if self.container_name is not None:
            _existing = existing
            _existing = _existing.get('template', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('containers[0]', {}) if isinstance(_existing, dict) else {}
            if _existing.get('name') != self.container_name:
                return True
        # Check container_image
        if self.container_image is not None:
            _existing = existing
            _existing = _existing.get('template', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('containers[0]', {}) if isinstance(_existing, dict) else {}
            if _existing.get('image') != self.container_image:
                return True
        # Check container_cpu
        if self.container_cpu is not None:
            _existing = existing
            _existing = _existing.get('template', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('containers[0]', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('resources', {}) if isinstance(_existing, dict) else {}
            if _existing.get('cpu') != self.container_cpu:
                return True
        # Check container_memory
        if self.container_memory is not None:
            _existing = existing
            _existing = _existing.get('template', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('containers[0]', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('resources', {}) if isinstance(_existing, dict) else {}
            if _existing.get('memory') != self.container_memory:
                return True
        # Check min_replicas
        if self.min_replicas is not None:
            _existing = existing
            _existing = _existing.get('template', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('scale', {}) if isinstance(_existing, dict) else {}
            if _existing.get('minReplicas') != self.min_replicas:
                return True
        # Check max_replicas
        if self.max_replicas is not None:
            _existing = existing
            _existing = _existing.get('template', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('scale', {}) if isinstance(_existing, dict) else {}
            if _existing.get('maxReplicas') != self.max_replicas:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2024-03-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2024-03-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2024-03-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.App/containerApps/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['managed_environment_id'] = response.get('managedEnvironmentId')
        _body = response
        _body = _body.get('configuration', {}) if isinstance(_body, dict) else {}
        result['revision_mode'] = _body.get('activeRevisionsMode')
        _body = response
        _body = _body.get('configuration', {}) if isinstance(_body, dict) else {}
        _body = _body.get('ingress', {}) if isinstance(_body, dict) else {}
        result['ingress_external'] = _body.get('external')
        _body = response
        _body = _body.get('configuration', {}) if isinstance(_body, dict) else {}
        _body = _body.get('ingress', {}) if isinstance(_body, dict) else {}
        result['ingress_target_port'] = _body.get('targetPort')
        _body = response
        _body = _body.get('configuration', {}) if isinstance(_body, dict) else {}
        _body = _body.get('ingress', {}) if isinstance(_body, dict) else {}
        result['ingress_transport'] = _body.get('transport')
        _body = response
        _body = _body.get('template', {}) if isinstance(_body, dict) else {}
        _body = _body.get('containers[0]', {}) if isinstance(_body, dict) else {}
        result['container_name'] = _body.get('name')
        _body = response
        _body = _body.get('template', {}) if isinstance(_body, dict) else {}
        _body = _body.get('containers[0]', {}) if isinstance(_body, dict) else {}
        result['container_image'] = _body.get('image')
        _body = response
        _body = _body.get('template', {}) if isinstance(_body, dict) else {}
        _body = _body.get('containers[0]', {}) if isinstance(_body, dict) else {}
        _body = _body.get('resources', {}) if isinstance(_body, dict) else {}
        result['container_cpu'] = _body.get('cpu')
        _body = response
        _body = _body.get('template', {}) if isinstance(_body, dict) else {}
        _body = _body.get('containers[0]', {}) if isinstance(_body, dict) else {}
        _body = _body.get('resources', {}) if isinstance(_body, dict) else {}
        result['container_memory'] = _body.get('memory')
        _body = response
        _body = _body.get('template', {}) if isinstance(_body, dict) else {}
        _body = _body.get('scale', {}) if isinstance(_body, dict) else {}
        result['min_replicas'] = _body.get('minReplicas')
        _body = response
        _body = _body.get('template', {}) if isinstance(_body, dict) else {}
        _body = _body.get('scale', {}) if isinstance(_body, dict) else {}
        result['max_replicas'] = _body.get('maxReplicas')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMContainerApp()


if __name__ == '__main__':
    main()
