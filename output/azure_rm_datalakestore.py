#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_datalakestore
version_added: "0.1.0"
short_description: Manage Azure Data Lake Storage Gen1 accounts.
description:
    - Create, update, or delete an Azure DataLakeStore.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the datalakestore.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    tier:
        description:
            - Commitment tier for the account.
        type: str
        choices:
            - "Consumption"
            - "Commitment_1TB"
            - "Commitment_10TB"
            - "Commitment_100TB"
            - "Commitment_500TB"
    encryption_state:
        description:
            - State of encryption for the account.
        type: str
        choices:
            - "Enabled"
            - "Disabled"
    firewall_state:
        description:
            - State of the firewall for the account.
        type: str
        choices:
            - "Enabled"
            - "Disabled"
    firewall_allow_azure_ips:
        description:
            - Whether to allow Azure service IPs through firewall.
        type: str
        choices:
            - "Enabled"
            - "Disabled"
    state:
        description:
            - Assert the state of the datalakestore.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure DataLakeStore REST API
      link: https://learn.microsoft.com/en-us/azure/data-lake-store/
'''

EXAMPLES = r'''
- name: Create DataLakeStore
  azure_rm_datalakestore:
    resource_group: myResourceGroup
    name: myDataLakeStore
    location: eastus
    state: present

- name: Delete DataLakeStore
  azure_rm_datalakestore:
    resource_group: myResourceGroup
    name: myDataLakeStore
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the datalakestore.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        tier:
            description: Commitment tier for the account.
            type: str
        encryption_state:
            description: State of encryption for the account.
            type: str
        firewall_state:
            description: State of the firewall for the account.
            type: str
        firewall_allow_azure_ips:
            description: Whether to allow Azure service IPs through firewall.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMDataLakeStore(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            tier=dict(
                type='str',
                choices=['Consumption', 'Commitment_1TB', 'Commitment_10TB', 'Commitment_100TB', 'Commitment_500TB'],
            ),
            encryption_state=dict(
                type='str',
                choices=['Enabled', 'Disabled'],
            ),
            firewall_state=dict(
                type='str',
                choices=['Enabled', 'Disabled'],
            ),
            firewall_allow_azure_ips=dict(
                type='str',
                choices=['Enabled', 'Disabled'],
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.tier = None
        self.encryption_state = None
        self.firewall_state = None
        self.firewall_allow_azure_ips = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.tier is not None:
            body['newTier'] = self.tier
        if self.encryption_state is not None:
            body['encryptionState'] = self.encryption_state
        if self.firewall_state is not None:
            body['firewallState'] = self.firewall_state
        if self.firewall_allow_azure_ips is not None:
            body['firewallAllowAzureIps'] = self.firewall_allow_azure_ips

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check tier
        if self.tier is not None:
            if existing.get('newTier') != self.tier:
                return True
        # Check encryption_state
        if self.encryption_state is not None:
            if existing.get('encryptionState') != self.encryption_state:
                return True
        # Check firewall_state
        if self.firewall_state is not None:
            if existing.get('firewallState') != self.firewall_state:
                return True
        # Check firewall_allow_azure_ips
        if self.firewall_allow_azure_ips is not None:
            if existing.get('firewallAllowAzureIps') != self.firewall_allow_azure_ips:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2016-11-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2016-11-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2016-11-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.DataLakeStore/accounts/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['tier'] = response.get('newTier')
        result['encryption_state'] = response.get('encryptionState')
        result['firewall_state'] = response.get('firewallState')
        result['firewall_allow_azure_ips'] = response.get('firewallAllowAzureIps')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMDataLakeStore()


if __name__ == '__main__':
    main()
