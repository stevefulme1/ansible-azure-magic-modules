#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_botservice
version_added: "0.1.0"
short_description: Manage Azure Bot Service instances.
description:
    - Create, update, or delete an Azure BotService.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the botservice.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    sku_name:
        description:
            - SKU name of the bot service.
        type: str
        required: true
        choices:
            - "F0"
            - "S1"
    kind:
        description:
            - The kind of bot service.
        type: str
        required: true
        choices:
            - "azurebot"
            - "sdk"
            - "designer"
            - "function"
            - "bot"
    display_name:
        description:
            - Display name of the bot.
        type: str
    endpoint:
        description:
            - Messaging endpoint URL for the bot.
        type: str
    msa_app_id:
        description:
            - Microsoft App ID for the bot.
        type: str
        required: true
    state:
        description:
            - Assert the state of the botservice.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure BotService REST API
      link: https://learn.microsoft.com/en-us/azure/bot-service/
'''

EXAMPLES = r'''
- name: Create BotService
  azure_rm_botservice:
    resource_group: myResourceGroup
    name: myBotService
    location: eastus
    sku_name: "example_value"
    kind: "example_value"
    msa_app_id: "example_value"
    state: present

- name: Delete BotService
  azure_rm_botservice:
    resource_group: myResourceGroup
    name: myBotService
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the botservice.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        sku_name:
            description: SKU name of the bot service.
            type: str
        kind:
            description: The kind of bot service.
            type: str
        display_name:
            description: Display name of the bot.
            type: str
        endpoint:
            description: Messaging endpoint URL for the bot.
            type: str
        msa_app_id:
            description: Microsoft App ID for the bot.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMBotService(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            sku_name=dict(
                type='str',
                required=True,
                choices=['F0', 'S1'],
            ),
            kind=dict(
                type='str',
                required=True,
                choices=['azurebot', 'sdk', 'designer', 'function', 'bot'],
            ),
            display_name=dict(
                type='str',
            ),
            endpoint=dict(
                type='str',
            ),
            msa_app_id=dict(
                type='str',
                required=True,
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.sku_name = None
        self.kind = None
        self.display_name = None
        self.endpoint = None
        self.msa_app_id = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.sku_name is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['name'] = self.sku_name
        if self.kind is not None:
            body['kind'] = self.kind

        properties = body.setdefault('properties', {})
        if self.display_name is not None:
            properties['displayName'] = self.display_name
        if self.endpoint is not None:
            properties['endpoint'] = self.endpoint
        if self.msa_app_id is not None:
            properties['msaAppId'] = self.msa_app_id

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check sku_name
        if self.sku_name is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('name') != self.sku_name:
                return True
        # Check kind
        if self.kind is not None:
            if existing.get('kind') != self.kind:
                return True
        # Check display_name
        if self.display_name is not None:
            if existing.get('properties', {}).get('displayName') != self.display_name:
                return True
        # Check endpoint
        if self.endpoint is not None:
            if existing.get('properties', {}).get('endpoint') != self.endpoint:
                return True
        # Check msa_app_id
        if self.msa_app_id is not None:
            if existing.get('properties', {}).get('msaAppId') != self.msa_app_id:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2023-09-15-preview'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2023-09-15-preview'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2023-09-15-preview'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.BotService/botServices/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_name'] = _body.get('name')
        result['kind'] = response.get('kind')
        result['display_name'] = response.get('properties', {}).get('displayName')
        result['endpoint'] = response.get('properties', {}).get('endpoint')
        result['msa_app_id'] = response.get('properties', {}).get('msaAppId')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMBotService()


if __name__ == '__main__':
    main()
