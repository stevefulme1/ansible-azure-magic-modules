#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_applicationinsights_info
version_added: "0.1.0"
short_description: Get facts for Azure ApplicationInsights
description:
    - Get facts for a specific ApplicationInsights or list all ApplicationInsightss in a resource group.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the applicationinsights.
            - If not specified, all applicationinsightss in the resource group will be listed.
        type: str
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure ApplicationInsights REST API
      link: https://learn.microsoft.com/en-us/azure/azure-monitor/app/app-insights-overview
'''

EXAMPLES = r'''
- name: Get facts for a specific ApplicationInsights
  azure_rm_applicationinsights_info:
    resource_group: myResourceGroup
    name: myApplicationInsights

- name: List all ApplicationInsightss in a resource group
  azure_rm_applicationinsights_info:
    resource_group: myResourceGroup
'''

RETURN = r'''
applicationinsightss:
    description:
        - List of applicationinsights dicts.
    returned: always
    type: list
    elements: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        application_type:
            description: Type of application being monitored.
            type: str
        kind:
            description: The kind of application.
            type: str
        flow_type:
            description: The flow type of the component.
            type: str
        request_source:
            description: The request source of the component.
            type: str
        workspace_resource_id:
            description: Resource ID of the Log Analytics workspace.
            type: str
        retention_in_days:
            description: Retention period in days.
            type: int
        sampling_percentage:
            description: Percentage of data to sample.
            type: float
        disable_ip_masking:
            description: Whether to disable IP address masking.
            type: bool
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMApplicationInsightsInfo(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str'),
        )

        self.resource_group = None
        self.name = None
        self.tags = None

        self.results = dict(
            changed=False,
            applicationinsightss=[],
        )

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=False,
            facts_module=True,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        result_key = 'applicationinsightss'

        if self.name:
            response = self.get_resource()
            if response:
                if self.has_tags(response.get('tags'), self.tags):
                    self.results[result_key] = [self.format_response(response)]
        else:
            responses = self.list_by_resource_group()
            for item in responses:
                if self.has_tags(item.get('tags'), self.tags):
                    self.results[result_key].append(self.format_response(item))

        return self.results

    def get_resource(self):
        try:
            url = (
                '/subscriptions/{subscription_id}'
                '/resourceGroups/{resource_group}'
                '/providers/Microsoft.Insights/components/{name}'
            ).format(
                subscription_id=self.subscription_id,
                resource_group=self.resource_group,
                name=self.name,
            )
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2020-02-02'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def list_by_resource_group(self):
        try:
            url = (
                '/subscriptions/{subscription_id}'
                '/resourceGroups/{resource_group}'
                '/providers/Microsoft.Insights/components'
            ).format(
                subscription_id=self.subscription_id,
                resource_group=self.resource_group,
            )
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2020-02-02'},
                None, None, [200], 0, 0,
            )
            result = self.deserialize_response(response)
            if result and isinstance(result, dict):
                return result.get('value', [])
            return []
        except Exception:
            return []

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['application_type'] = response.get('properties', {}).get('Application_Type')
        result['kind'] = response.get('kind')
        result['flow_type'] = response.get('properties', {}).get('Flow_Type')
        result['request_source'] = response.get('properties', {}).get('Request_Source')
        result['workspace_resource_id'] = response.get('properties', {}).get('WorkspaceResourceId')
        result['retention_in_days'] = response.get('properties', {}).get('RetentionInDays')
        result['sampling_percentage'] = response.get('properties', {}).get('SamplingPercentage')
        result['disable_ip_masking'] = response.get('properties', {}).get('DisableIpMasking')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMApplicationInsightsInfo()


if __name__ == '__main__':
    main()
