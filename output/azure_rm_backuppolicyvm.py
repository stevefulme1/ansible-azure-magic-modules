#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_backuppolicyvm
version_added: "0.1.0"
short_description: Manage Azure Backup Policy for Virtual Machines.
description:
    - Create, update, or delete an Azure BackupPolicyVM.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the backuppolicyvm.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    vault_name:
        description:
            - Name of the Recovery Services Vault.
        type: str
        required: true
    timezone:
        description:
            - Timezone for the backup schedule.
        type: str
    instant_rp_retention_range_in_days:
        description:
            - Instant recovery point retention in days.
        type: int
    daily_schedule_time:
        description:
            - Time for daily backup schedule.
        type: str
    daily_retention_count:
        description:
            - Number of daily backups to retain.
        type: int
    weekly_retention_count:
        description:
            - Number of weekly backups to retain.
        type: int
    weekly_retention_days:
        description:
            - Days of the week for weekly retention.
        type: list
    monthly_retention_count:
        description:
            - Number of monthly backups to retain.
        type: int
    yearly_retention_count:
        description:
            - Number of yearly backups to retain.
        type: int
    state:
        description:
            - Assert the state of the backuppolicyvm.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure BackupPolicyVM REST API
      link: https://learn.microsoft.com/en-us/azure/backup/backup-azure-arm-userestapi-createorupdatepolicy
'''

EXAMPLES = r'''
- name: Create BackupPolicyVM
  azure_rm_backuppolicyvm:
    resource_group: myResourceGroup
    name: myBackupPolicyVM
    location: eastus
    vault_name: "example_value"
    state: present

- name: Delete BackupPolicyVM
  azure_rm_backuppolicyvm:
    resource_group: myResourceGroup
    name: myBackupPolicyVM
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the backuppolicyvm.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        vault_name:
            description: Name of the Recovery Services Vault.
            type: str
        timezone:
            description: Timezone for the backup schedule.
            type: str
        instant_rp_retention_range_in_days:
            description: Instant recovery point retention in days.
            type: int
        daily_schedule_time:
            description: Time for daily backup schedule.
            type: str
        daily_retention_count:
            description: Number of daily backups to retain.
            type: int
        weekly_retention_count:
            description: Number of weekly backups to retain.
            type: int
        weekly_retention_days:
            description: Days of the week for weekly retention.
            type: list
        monthly_retention_count:
            description: Number of monthly backups to retain.
            type: int
        yearly_retention_count:
            description: Number of yearly backups to retain.
            type: int
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMBackupPolicyVM(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            vault_name=dict(
                type='str',
                required=True,
            ),
            timezone=dict(
                type='str',
            ),
            instant_rp_retention_range_in_days=dict(
                type='int',
            ),
            daily_schedule_time=dict(
                type='str',
            ),
            daily_retention_count=dict(
                type='int',
            ),
            weekly_retention_count=dict(
                type='int',
            ),
            weekly_retention_days=dict(
                type='list',
            ),
            monthly_retention_count=dict(
                type='int',
            ),
            yearly_retention_count=dict(
                type='int',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.vault_name = None
        self.timezone = None
        self.instant_rp_retention_range_in_days = None
        self.daily_schedule_time = None
        self.daily_retention_count = None
        self.weekly_retention_count = None
        self.weekly_retention_days = None
        self.monthly_retention_count = None
        self.yearly_retention_count = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.vault_name is not None:
            body['VaultName'] = self.vault_name
        if self.timezone is not None:
            body['timeZone'] = self.timezone
        if self.instant_rp_retention_range_in_days is not None:
            body['instantRpRetentionRangeInDays'] = self.instant_rp_retention_range_in_days
        if self.daily_schedule_time is not None:
            _target = body
            _target = _target.setdefault('schedulePolicy', {})
            _target['scheduleRunTimes'] = self.daily_schedule_time
        if self.daily_retention_count is not None:
            _target = body
            _target = _target.setdefault('retentionPolicy', {})
            _target = _target.setdefault('dailySchedule', {})
            _target = _target.setdefault('retentionDuration', {})
            _target['count'] = self.daily_retention_count
        if self.weekly_retention_count is not None:
            _target = body
            _target = _target.setdefault('retentionPolicy', {})
            _target = _target.setdefault('weeklySchedule', {})
            _target = _target.setdefault('retentionDuration', {})
            _target['count'] = self.weekly_retention_count
        if self.weekly_retention_days is not None:
            _target = body
            _target = _target.setdefault('retentionPolicy', {})
            _target = _target.setdefault('weeklySchedule', {})
            _target['daysOfTheWeek'] = self.weekly_retention_days
        if self.monthly_retention_count is not None:
            _target = body
            _target = _target.setdefault('retentionPolicy', {})
            _target = _target.setdefault('monthlySchedule', {})
            _target = _target.setdefault('retentionDuration', {})
            _target['count'] = self.monthly_retention_count
        if self.yearly_retention_count is not None:
            _target = body
            _target = _target.setdefault('retentionPolicy', {})
            _target = _target.setdefault('yearlySchedule', {})
            _target = _target.setdefault('retentionDuration', {})
            _target['count'] = self.yearly_retention_count

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check vault_name
        if self.vault_name is not None:
            if existing.get('VaultName') != self.vault_name:
                return True
        # Check timezone
        if self.timezone is not None:
            if existing.get('timeZone') != self.timezone:
                return True
        # Check instant_rp_retention_range_in_days
        if self.instant_rp_retention_range_in_days is not None:
            if existing.get('instantRpRetentionRangeInDays') != self.instant_rp_retention_range_in_days:
                return True
        # Check daily_schedule_time
        if self.daily_schedule_time is not None:
            _existing = existing
            _existing = _existing.get('schedulePolicy', {}) if isinstance(_existing, dict) else {}
            if _existing.get('scheduleRunTimes') != self.daily_schedule_time:
                return True
        # Check daily_retention_count
        if self.daily_retention_count is not None:
            _existing = existing
            _existing = _existing.get('retentionPolicy', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('dailySchedule', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('retentionDuration', {}) if isinstance(_existing, dict) else {}
            if _existing.get('count') != self.daily_retention_count:
                return True
        # Check weekly_retention_count
        if self.weekly_retention_count is not None:
            _existing = existing
            _existing = _existing.get('retentionPolicy', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('weeklySchedule', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('retentionDuration', {}) if isinstance(_existing, dict) else {}
            if _existing.get('count') != self.weekly_retention_count:
                return True
        # Check weekly_retention_days
        if self.weekly_retention_days is not None:
            _existing = existing
            _existing = _existing.get('retentionPolicy', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('weeklySchedule', {}) if isinstance(_existing, dict) else {}
            if _existing.get('daysOfTheWeek') != self.weekly_retention_days:
                return True
        # Check monthly_retention_count
        if self.monthly_retention_count is not None:
            _existing = existing
            _existing = _existing.get('retentionPolicy', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('monthlySchedule', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('retentionDuration', {}) if isinstance(_existing, dict) else {}
            if _existing.get('count') != self.monthly_retention_count:
                return True
        # Check yearly_retention_count
        if self.yearly_retention_count is not None:
            _existing = existing
            _existing = _existing.get('retentionPolicy', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('yearlySchedule', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('retentionDuration', {}) if isinstance(_existing, dict) else {}
            if _existing.get('count') != self.yearly_retention_count:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2024-04-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2024-04-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2024-04-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.RecoveryServices/vaults/backupPolicies/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['vault_name'] = response.get('VaultName')
        result['timezone'] = response.get('timeZone')
        result['instant_rp_retention_range_in_days'] = response.get('instantRpRetentionRangeInDays')
        _body = response
        _body = _body.get('schedulePolicy', {}) if isinstance(_body, dict) else {}
        result['daily_schedule_time'] = _body.get('scheduleRunTimes')
        _body = response
        _body = _body.get('retentionPolicy', {}) if isinstance(_body, dict) else {}
        _body = _body.get('dailySchedule', {}) if isinstance(_body, dict) else {}
        _body = _body.get('retentionDuration', {}) if isinstance(_body, dict) else {}
        result['daily_retention_count'] = _body.get('count')
        _body = response
        _body = _body.get('retentionPolicy', {}) if isinstance(_body, dict) else {}
        _body = _body.get('weeklySchedule', {}) if isinstance(_body, dict) else {}
        _body = _body.get('retentionDuration', {}) if isinstance(_body, dict) else {}
        result['weekly_retention_count'] = _body.get('count')
        _body = response
        _body = _body.get('retentionPolicy', {}) if isinstance(_body, dict) else {}
        _body = _body.get('weeklySchedule', {}) if isinstance(_body, dict) else {}
        result['weekly_retention_days'] = _body.get('daysOfTheWeek')
        _body = response
        _body = _body.get('retentionPolicy', {}) if isinstance(_body, dict) else {}
        _body = _body.get('monthlySchedule', {}) if isinstance(_body, dict) else {}
        _body = _body.get('retentionDuration', {}) if isinstance(_body, dict) else {}
        result['monthly_retention_count'] = _body.get('count')
        _body = response
        _body = _body.get('retentionPolicy', {}) if isinstance(_body, dict) else {}
        _body = _body.get('yearlySchedule', {}) if isinstance(_body, dict) else {}
        _body = _body.get('retentionDuration', {}) if isinstance(_body, dict) else {}
        result['yearly_retention_count'] = _body.get('count')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMBackupPolicyVM()


if __name__ == '__main__':
    main()
