#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_virtualmachinescaleset
version_added: "0.1.0"
short_description: Manage Azure virtual machine scale set instances.
description:
    - Create, update, or delete an Azure VirtualMachineScaleSet.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the virtualmachinescaleset.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    sku_name:
        description:
            - Name of the VM SKU for scale set instances.
        type: str
    sku_tier:
        description:
            - Tier of the VM SKU for scale set instances.
        type: str
    sku_capacity:
        description:
            - Number of instances in the scale set.
        type: int
    upgrade_policy_mode:
        description:
            - Upgrade policy mode for the scale set.
        type: str
        choices:
            - "Manual"
            - "Rolling"
            - "Automatic"
    admin_username:
        description:
            - Administrator username for scale set instances.
        type: str
    admin_password:
        description:
            - Administrator password for scale set instances.
        type: str
    image_publisher:
        description:
            - Publisher of the VM image.
        type: str
    image_offer:
        description:
            - Offer of the VM image.
        type: str
    image_sku:
        description:
            - SKU of the VM image.
        type: str
    image_version:
        description:
            - Version of the VM image.
        type: str
        default: "latest"
    os_disk_type:
        description:
            - Managed disk storage account type for the OS disk.
        type: str
        choices:
            - "Premium_LRS"
            - "StandardSSD_LRS"
            - "Standard_LRS"
    single_placement_group:
        description:
            - Whether the scale set is limited to a single placement group.
        type: bool
    zone_balance:
        description:
            - Whether to balance instances across availability zones.
        type: bool
    zones:
        description:
            - List of availability zones for the scale set.
        type: list
    state:
        description:
            - Assert the state of the virtualmachinescaleset.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure VirtualMachineScaleSet REST API
      link: https://learn.microsoft.com/en-us/azure/virtual-machine-scale-sets/
'''

EXAMPLES = r'''
- name: Create VirtualMachineScaleSet
  azure_rm_virtualmachinescaleset:
    resource_group: myResourceGroup
    name: myVirtualMachineScaleSet
    location: eastus
    state: present

- name: Delete VirtualMachineScaleSet
  azure_rm_virtualmachinescaleset:
    resource_group: myResourceGroup
    name: myVirtualMachineScaleSet
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the virtualmachinescaleset.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        sku_name:
            description: Name of the VM SKU for scale set instances.
            type: str
        sku_tier:
            description: Tier of the VM SKU for scale set instances.
            type: str
        sku_capacity:
            description: Number of instances in the scale set.
            type: int
        upgrade_policy_mode:
            description: Upgrade policy mode for the scale set.
            type: str
        admin_username:
            description: Administrator username for scale set instances.
            type: str
        admin_password:
            description: Administrator password for scale set instances.
            type: str
        image_publisher:
            description: Publisher of the VM image.
            type: str
        image_offer:
            description: Offer of the VM image.
            type: str
        image_sku:
            description: SKU of the VM image.
            type: str
        image_version:
            description: Version of the VM image.
            type: str
        os_disk_type:
            description: Managed disk storage account type for the OS disk.
            type: str
        single_placement_group:
            description: Whether the scale set is limited to a single placement group.
            type: bool
        zone_balance:
            description: Whether to balance instances across availability zones.
            type: bool
        zones:
            description: List of availability zones for the scale set.
            type: list
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMVirtualMachineScaleSet(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            sku_name=dict(
                type='str',
            ),
            sku_tier=dict(
                type='str',
            ),
            sku_capacity=dict(
                type='int',
            ),
            upgrade_policy_mode=dict(
                type='str',
                choices=['Manual', 'Rolling', 'Automatic'],
            ),
            admin_username=dict(
                type='str',
            ),
            admin_password=dict(
                type='str',
            ),
            image_publisher=dict(
                type='str',
            ),
            image_offer=dict(
                type='str',
            ),
            image_sku=dict(
                type='str',
            ),
            image_version=dict(
                type='str',
                default='latest',
            ),
            os_disk_type=dict(
                type='str',
                choices=['Premium_LRS', 'StandardSSD_LRS', 'Standard_LRS'],
            ),
            single_placement_group=dict(
                type='bool',
            ),
            zone_balance=dict(
                type='bool',
            ),
            zones=dict(
                type='list',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.sku_name = None
        self.sku_tier = None
        self.sku_capacity = None
        self.upgrade_policy_mode = None
        self.admin_username = None
        self.admin_password = None
        self.image_publisher = None
        self.image_offer = None
        self.image_sku = None
        self.image_version = None
        self.os_disk_type = None
        self.single_placement_group = None
        self.zone_balance = None
        self.zones = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.sku_name is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['name'] = self.sku_name
        if self.sku_tier is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['tier'] = self.sku_tier
        if self.sku_capacity is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['capacity'] = self.sku_capacity
        if self.upgrade_policy_mode is not None:
            _target = body
            _target = _target.setdefault('upgradePolicy', {})
            _target['mode'] = self.upgrade_policy_mode
        if self.admin_username is not None:
            _target = body
            _target = _target.setdefault('virtualMachineProfile', {})
            _target = _target.setdefault('osProfile', {})
            _target['adminUsername'] = self.admin_username
        if self.admin_password is not None:
            _target = body
            _target = _target.setdefault('virtualMachineProfile', {})
            _target = _target.setdefault('osProfile', {})
            _target['adminPassword'] = self.admin_password
        if self.image_publisher is not None:
            _target = body
            _target = _target.setdefault('virtualMachineProfile', {})
            _target = _target.setdefault('storageProfile', {})
            _target = _target.setdefault('imageReference', {})
            _target['publisher'] = self.image_publisher
        if self.image_offer is not None:
            _target = body
            _target = _target.setdefault('virtualMachineProfile', {})
            _target = _target.setdefault('storageProfile', {})
            _target = _target.setdefault('imageReference', {})
            _target['offer'] = self.image_offer
        if self.image_sku is not None:
            _target = body
            _target = _target.setdefault('virtualMachineProfile', {})
            _target = _target.setdefault('storageProfile', {})
            _target = _target.setdefault('imageReference', {})
            _target['sku'] = self.image_sku
        if self.image_version is not None:
            _target = body
            _target = _target.setdefault('virtualMachineProfile', {})
            _target = _target.setdefault('storageProfile', {})
            _target = _target.setdefault('imageReference', {})
            _target['version'] = self.image_version
        if self.os_disk_type is not None:
            _target = body
            _target = _target.setdefault('virtualMachineProfile', {})
            _target = _target.setdefault('storageProfile', {})
            _target = _target.setdefault('osDisk', {})
            _target = _target.setdefault('managedDisk', {})
            _target['storageAccountType'] = self.os_disk_type
        if self.single_placement_group is not None:
            body['singlePlacementGroup'] = self.single_placement_group
        if self.zone_balance is not None:
            body['zoneBalance'] = self.zone_balance
        if self.zones is not None:
            body['zones'] = self.zones

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check sku_name
        if self.sku_name is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('name') != self.sku_name:
                return True
        # Check sku_tier
        if self.sku_tier is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('tier') != self.sku_tier:
                return True
        # Check sku_capacity
        if self.sku_capacity is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('capacity') != self.sku_capacity:
                return True
        # Check upgrade_policy_mode
        if self.upgrade_policy_mode is not None:
            _existing = existing
            _existing = _existing.get('upgradePolicy', {}) if isinstance(_existing, dict) else {}
            if _existing.get('mode') != self.upgrade_policy_mode:
                return True
        # Check admin_username
        if self.admin_username is not None:
            _existing = existing
            _existing = _existing.get('virtualMachineProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('osProfile', {}) if isinstance(_existing, dict) else {}
            if _existing.get('adminUsername') != self.admin_username:
                return True
        # Check admin_password
        if self.admin_password is not None:
            _existing = existing
            _existing = _existing.get('virtualMachineProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('osProfile', {}) if isinstance(_existing, dict) else {}
            if _existing.get('adminPassword') != self.admin_password:
                return True
        # Check image_publisher
        if self.image_publisher is not None:
            _existing = existing
            _existing = _existing.get('virtualMachineProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('storageProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('imageReference', {}) if isinstance(_existing, dict) else {}
            if _existing.get('publisher') != self.image_publisher:
                return True
        # Check image_offer
        if self.image_offer is not None:
            _existing = existing
            _existing = _existing.get('virtualMachineProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('storageProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('imageReference', {}) if isinstance(_existing, dict) else {}
            if _existing.get('offer') != self.image_offer:
                return True
        # Check image_sku
        if self.image_sku is not None:
            _existing = existing
            _existing = _existing.get('virtualMachineProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('storageProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('imageReference', {}) if isinstance(_existing, dict) else {}
            if _existing.get('sku') != self.image_sku:
                return True
        # Check image_version
        if self.image_version is not None:
            _existing = existing
            _existing = _existing.get('virtualMachineProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('storageProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('imageReference', {}) if isinstance(_existing, dict) else {}
            if _existing.get('version') != self.image_version:
                return True
        # Check os_disk_type
        if self.os_disk_type is not None:
            _existing = existing
            _existing = _existing.get('virtualMachineProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('storageProfile', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('osDisk', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('managedDisk', {}) if isinstance(_existing, dict) else {}
            if _existing.get('storageAccountType') != self.os_disk_type:
                return True
        # Check single_placement_group
        if self.single_placement_group is not None:
            if existing.get('singlePlacementGroup') != self.single_placement_group:
                return True
        # Check zone_balance
        if self.zone_balance is not None:
            if existing.get('zoneBalance') != self.zone_balance:
                return True
        # Check zones
        if self.zones is not None:
            if existing.get('zones') != self.zones:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2024-03-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2024-03-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2024-03-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Compute/virtualMachineScaleSets/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_name'] = _body.get('name')
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_tier'] = _body.get('tier')
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_capacity'] = _body.get('capacity')
        _body = response
        _body = _body.get('upgradePolicy', {}) if isinstance(_body, dict) else {}
        result['upgrade_policy_mode'] = _body.get('mode')
        _body = response
        _body = _body.get('virtualMachineProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('osProfile', {}) if isinstance(_body, dict) else {}
        result['admin_username'] = _body.get('adminUsername')
        _body = response
        _body = _body.get('virtualMachineProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('osProfile', {}) if isinstance(_body, dict) else {}
        result['admin_password'] = _body.get('adminPassword')
        _body = response
        _body = _body.get('virtualMachineProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('storageProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('imageReference', {}) if isinstance(_body, dict) else {}
        result['image_publisher'] = _body.get('publisher')
        _body = response
        _body = _body.get('virtualMachineProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('storageProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('imageReference', {}) if isinstance(_body, dict) else {}
        result['image_offer'] = _body.get('offer')
        _body = response
        _body = _body.get('virtualMachineProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('storageProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('imageReference', {}) if isinstance(_body, dict) else {}
        result['image_sku'] = _body.get('sku')
        _body = response
        _body = _body.get('virtualMachineProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('storageProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('imageReference', {}) if isinstance(_body, dict) else {}
        result['image_version'] = _body.get('version')
        _body = response
        _body = _body.get('virtualMachineProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('storageProfile', {}) if isinstance(_body, dict) else {}
        _body = _body.get('osDisk', {}) if isinstance(_body, dict) else {}
        _body = _body.get('managedDisk', {}) if isinstance(_body, dict) else {}
        result['os_disk_type'] = _body.get('storageAccountType')
        result['single_placement_group'] = response.get('singlePlacementGroup')
        result['zone_balance'] = response.get('zoneBalance')
        result['zones'] = response.get('zones')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMVirtualMachineScaleSet()


if __name__ == '__main__':
    main()
