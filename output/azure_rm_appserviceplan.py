#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_appserviceplan
version_added: "0.1.0"
short_description: Manage Azure App Service Plan instances.
description:
    - Create, update, or delete an Azure AppServicePlan.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the appserviceplan.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    sku_name:
        description:
            - SKU name of the App Service Plan.
        type: str
        choices:
            - "F1"
            - "D1"
            - "B1"
            - "B2"
            - "B3"
            - "S1"
            - "S2"
            - "S3"
            - "P1v2"
            - "P2v2"
            - "P3v2"
            - "P1v3"
            - "P2v3"
            - "P3v3"
            - "I1"
            - "I2"
            - "I3"
            - "I1v2"
            - "I2v2"
            - "I3v2"
            - "Y1"
            - "EP1"
            - "EP2"
            - "EP3"
    sku_tier:
        description:
            - SKU tier of the App Service Plan.
        type: str
        choices:
            - "Free"
            - "Shared"
            - "Basic"
            - "Standard"
            - "Premium"
            - "PremiumV2"
            - "PremiumV3"
            - "Isolated"
            - "IsolatedV2"
            - "Dynamic"
            - "ElasticPremium"
    kind:
        description:
            - Kind of the App Service Plan.
        type: str
        choices:
            - "app"
            - "linux"
            - "elastic"
            - "functionapp"
    reserved:
        description:
            - Whether the plan is Linux. Set to true for Linux.
        type: bool
    per_site_scaling:
        description:
            - Whether per-app scaling is enabled.
        type: bool
    maximum_elastic_worker_count:
        description:
            - Maximum number of elastic workers for the plan.
        type: int
    zone_redundant:
        description:
            - Whether the plan is zone redundant.
        type: bool
    state:
        description:
            - Assert the state of the appserviceplan.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure AppServicePlan REST API
      link: https://learn.microsoft.com/en-us/azure/app-service/overview-hosting-plans
'''

EXAMPLES = r'''
- name: Create AppServicePlan
  azure_rm_appserviceplan:
    resource_group: myResourceGroup
    name: myAppServicePlan
    location: eastus
    state: present

- name: Delete AppServicePlan
  azure_rm_appserviceplan:
    resource_group: myResourceGroup
    name: myAppServicePlan
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the appserviceplan.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        sku_name:
            description: SKU name of the App Service Plan.
            type: str
        sku_tier:
            description: SKU tier of the App Service Plan.
            type: str
        kind:
            description: Kind of the App Service Plan.
            type: str
        reserved:
            description: Whether the plan is Linux. Set to true for Linux.
            type: bool
        per_site_scaling:
            description: Whether per-app scaling is enabled.
            type: bool
        maximum_elastic_worker_count:
            description: Maximum number of elastic workers for the plan.
            type: int
        zone_redundant:
            description: Whether the plan is zone redundant.
            type: bool
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMAppServicePlan(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            sku_name=dict(
                type='str',
                choices=[
                    'F1',
                    'D1',
                    'B1',
                    'B2',
                    'B3',
                    'S1',
                    'S2',
                    'S3',
                    'P1v2',
                    'P2v2',
                    'P3v2',
                    'P1v3',
                    'P2v3',
                    'P3v3',
                    'I1',
                    'I2',
                    'I3',
                    'I1v2',
                    'I2v2',
                    'I3v2',
                    'Y1',
                    'EP1',
                    'EP2',
                    'EP3',
                ],
            ),
            sku_tier=dict(
                type='str',
                choices=['Free', 'Shared', 'Basic', 'Standard', 'Premium', 'PremiumV2', 'PremiumV3', 'Isolated', 'IsolatedV2', 'Dynamic', 'ElasticPremium'],
            ),
            kind=dict(
                type='str',
                choices=['app', 'linux', 'elastic', 'functionapp'],
            ),
            reserved=dict(
                type='bool',
            ),
            per_site_scaling=dict(
                type='bool',
            ),
            maximum_elastic_worker_count=dict(
                type='int',
            ),
            zone_redundant=dict(
                type='bool',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.sku_name = None
        self.sku_tier = None
        self.kind = None
        self.reserved = None
        self.per_site_scaling = None
        self.maximum_elastic_worker_count = None
        self.zone_redundant = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.sku_name is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['name'] = self.sku_name
        if self.sku_tier is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['tier'] = self.sku_tier
        if self.kind is not None:
            body['kind'] = self.kind
        if self.reserved is not None:
            body['reserved'] = self.reserved
        if self.per_site_scaling is not None:
            body['perSiteScaling'] = self.per_site_scaling
        if self.maximum_elastic_worker_count is not None:
            body['maximumElasticWorkerCount'] = self.maximum_elastic_worker_count
        if self.zone_redundant is not None:
            body['zoneRedundant'] = self.zone_redundant

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check sku_name
        if self.sku_name is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('name') != self.sku_name:
                return True
        # Check sku_tier
        if self.sku_tier is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('tier') != self.sku_tier:
                return True
        # Check kind
        if self.kind is not None:
            if existing.get('kind') != self.kind:
                return True
        # Check reserved
        if self.reserved is not None:
            if existing.get('reserved') != self.reserved:
                return True
        # Check per_site_scaling
        if self.per_site_scaling is not None:
            if existing.get('perSiteScaling') != self.per_site_scaling:
                return True
        # Check maximum_elastic_worker_count
        if self.maximum_elastic_worker_count is not None:
            if existing.get('maximumElasticWorkerCount') != self.maximum_elastic_worker_count:
                return True
        # Check zone_redundant
        if self.zone_redundant is not None:
            if existing.get('zoneRedundant') != self.zone_redundant:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2023-12-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2023-12-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2023-12-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Web/serverfarms/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_name'] = _body.get('name')
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_tier'] = _body.get('tier')
        result['kind'] = response.get('kind')
        result['reserved'] = response.get('reserved')
        result['per_site_scaling'] = response.get('perSiteScaling')
        result['maximum_elastic_worker_count'] = response.get('maximumElasticWorkerCount')
        result['zone_redundant'] = response.get('zoneRedundant')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMAppServicePlan()


if __name__ == '__main__':
    main()
