#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_roledefinition
version_added: "0.1.0"
short_description: Manage Azure custom role definitions.
description:
    - Create, update, or delete an Azure RoleDefinition.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the roledefinition.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    scope:
        description:
            - The scope of the role definition.
        type: str
        required: true
    role_name:
        description:
            - The display name of the role definition.
        type: str
        required: true
    description:
        description:
            - Description of the role definition.
        type: str
    permissions_actions:
        description:
            - Allowed actions for the role.
        type: list
    permissions_not_actions:
        description:
            - Denied actions for the role.
        type: list
    permissions_data_actions:
        description:
            - Allowed data actions for the role.
        type: list
    permissions_not_data_actions:
        description:
            - Denied data actions for the role.
        type: list
    assignable_scopes:
        description:
            - Scopes where the role can be assigned.
        type: list
    state:
        description:
            - Assert the state of the roledefinition.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure RoleDefinition REST API
      link: https://learn.microsoft.com/en-us/azure/role-based-access-control/custom-roles
'''

EXAMPLES = r'''
- name: Create RoleDefinition
  azure_rm_roledefinition:
    resource_group: myResourceGroup
    name: myRoleDefinition
    location: eastus
    scope: "example_value"
    role_name: "example_value"
    state: present

- name: Delete RoleDefinition
  azure_rm_roledefinition:
    resource_group: myResourceGroup
    name: myRoleDefinition
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the roledefinition.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        scope:
            description: The scope of the role definition.
            type: str
        role_name:
            description: The display name of the role definition.
            type: str
        description:
            description: Description of the role definition.
            type: str
        permissions_actions:
            description: Allowed actions for the role.
            type: list
        permissions_not_actions:
            description: Denied actions for the role.
            type: list
        permissions_data_actions:
            description: Allowed data actions for the role.
            type: list
        permissions_not_data_actions:
            description: Denied data actions for the role.
            type: list
        assignable_scopes:
            description: Scopes where the role can be assigned.
            type: list
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMRoleDefinition(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            scope=dict(
                type='str',
                required=True,
            ),
            role_name=dict(
                type='str',
                required=True,
            ),
            description=dict(
                type='str',
            ),
            permissions_actions=dict(
                type='list',
            ),
            permissions_not_actions=dict(
                type='list',
            ),
            permissions_data_actions=dict(
                type='list',
            ),
            permissions_not_data_actions=dict(
                type='list',
            ),
            assignable_scopes=dict(
                type='list',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.scope = None
        self.role_name = None
        self.description = None
        self.permissions_actions = None
        self.permissions_not_actions = None
        self.permissions_data_actions = None
        self.permissions_not_data_actions = None
        self.assignable_scopes = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.scope is not None:
            body['scope'] = self.scope

        properties = body.setdefault('properties', {})
        if self.role_name is not None:
            properties['roleName'] = self.role_name
        if self.description is not None:
            properties['description'] = self.description
        if self.permissions_actions is not None:
            _target = properties
            _target = _target.setdefault('permissions[0]', {})
            _target['actions'] = self.permissions_actions
        if self.permissions_not_actions is not None:
            _target = properties
            _target = _target.setdefault('permissions[0]', {})
            _target['notActions'] = self.permissions_not_actions
        if self.permissions_data_actions is not None:
            _target = properties
            _target = _target.setdefault('permissions[0]', {})
            _target['dataActions'] = self.permissions_data_actions
        if self.permissions_not_data_actions is not None:
            _target = properties
            _target = _target.setdefault('permissions[0]', {})
            _target['notDataActions'] = self.permissions_not_data_actions
        if self.assignable_scopes is not None:
            properties['assignableScopes'] = self.assignable_scopes

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check scope
        if self.scope is not None:
            if existing.get('scope') != self.scope:
                return True
        # Check role_name
        if self.role_name is not None:
            if existing.get('properties', {}).get('roleName') != self.role_name:
                return True
        # Check description
        if self.description is not None:
            if existing.get('properties', {}).get('description') != self.description:
                return True
        # Check permissions_actions
        if self.permissions_actions is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('permissions[0]', {}) if isinstance(_existing, dict) else {}
            if _existing.get('actions') != self.permissions_actions:
                return True
        # Check permissions_not_actions
        if self.permissions_not_actions is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('permissions[0]', {}) if isinstance(_existing, dict) else {}
            if _existing.get('notActions') != self.permissions_not_actions:
                return True
        # Check permissions_data_actions
        if self.permissions_data_actions is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('permissions[0]', {}) if isinstance(_existing, dict) else {}
            if _existing.get('dataActions') != self.permissions_data_actions:
                return True
        # Check permissions_not_data_actions
        if self.permissions_not_data_actions is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('permissions[0]', {}) if isinstance(_existing, dict) else {}
            if _existing.get('notDataActions') != self.permissions_not_data_actions:
                return True
        # Check assignable_scopes
        if self.assignable_scopes is not None:
            if existing.get('properties', {}).get('assignableScopes') != self.assignable_scopes:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2022-04-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2022-04-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2022-04-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Authorization/roleDefinitions/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['scope'] = response.get('scope')
        result['role_name'] = response.get('properties', {}).get('roleName')
        result['description'] = response.get('properties', {}).get('description')
        _props = response.get('properties', {})
        _props = _props.get('permissions[0]', {}) if isinstance(_props, dict) else {}
        result['permissions_actions'] = _props.get('actions')
        _props = response.get('properties', {})
        _props = _props.get('permissions[0]', {}) if isinstance(_props, dict) else {}
        result['permissions_not_actions'] = _props.get('notActions')
        _props = response.get('properties', {})
        _props = _props.get('permissions[0]', {}) if isinstance(_props, dict) else {}
        result['permissions_data_actions'] = _props.get('dataActions')
        _props = response.get('properties', {})
        _props = _props.get('permissions[0]', {}) if isinstance(_props, dict) else {}
        result['permissions_not_data_actions'] = _props.get('notDataActions')
        result['assignable_scopes'] = response.get('properties', {}).get('assignableScopes')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMRoleDefinition()


if __name__ == '__main__':
    main()
