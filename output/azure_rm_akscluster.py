#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_akscluster
version_added: "0.1.0"
short_description: Manage Azure Kubernetes Service managed clusters.
description:
    - Create, update, or delete an Azure AksCluster.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the akscluster.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    dns_prefix:
        description:
            - DNS prefix for the cluster.
        type: str
    kubernetes_version:
        description:
            - Kubernetes version for the cluster.
        type: str
    node_resource_group:
        description:
            - Name of the resource group for cluster nodes.
        type: str
    network_plugin:
        description:
            - Network plugin for the cluster.
        type: str
        choices:
            - "azure"
            - "kubenet"
            - "none"
    network_policy:
        description:
            - Network policy for the cluster.
        type: str
        choices:
            - "azure"
            - "calico"
    service_cidr:
        description:
            - CIDR range for Kubernetes services.
        type: str
    dns_service_ip:
        description:
            - IP address for the Kubernetes DNS service.
        type: str
    default_node_pool_name:
        description:
            - Name of the default node pool.
        type: str
        default: "default"
    default_node_pool_vm_size:
        description:
            - VM size for the default node pool.
        type: str
    default_node_pool_count:
        description:
            - Number of nodes in the default node pool.
        type: int
    default_node_pool_min_count:
        description:
            - Minimum number of nodes for autoscaling.
        type: int
    default_node_pool_max_count:
        description:
            - Maximum number of nodes for autoscaling.
        type: int
    enable_rbac:
        description:
            - Whether to enable Kubernetes RBAC.
        type: bool
        default: "True"
    identity_type:
        description:
            - Type of managed identity for the cluster.
        type: str
        choices:
            - "SystemAssigned"
            - "UserAssigned"
            - "None"
    state:
        description:
            - Assert the state of the akscluster.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure AksCluster REST API
      link: https://learn.microsoft.com/en-us/azure/aks/
'''

EXAMPLES = r'''
- name: Create AksCluster
  azure_rm_akscluster:
    resource_group: myResourceGroup
    name: myAksCluster
    location: eastus
    state: present

- name: Delete AksCluster
  azure_rm_akscluster:
    resource_group: myResourceGroup
    name: myAksCluster
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the akscluster.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        dns_prefix:
            description: DNS prefix for the cluster.
            type: str
        kubernetes_version:
            description: Kubernetes version for the cluster.
            type: str
        node_resource_group:
            description: Name of the resource group for cluster nodes.
            type: str
        network_plugin:
            description: Network plugin for the cluster.
            type: str
        network_policy:
            description: Network policy for the cluster.
            type: str
        service_cidr:
            description: CIDR range for Kubernetes services.
            type: str
        dns_service_ip:
            description: IP address for the Kubernetes DNS service.
            type: str
        default_node_pool_name:
            description: Name of the default node pool.
            type: str
        default_node_pool_vm_size:
            description: VM size for the default node pool.
            type: str
        default_node_pool_count:
            description: Number of nodes in the default node pool.
            type: int
        default_node_pool_min_count:
            description: Minimum number of nodes for autoscaling.
            type: int
        default_node_pool_max_count:
            description: Maximum number of nodes for autoscaling.
            type: int
        enable_rbac:
            description: Whether to enable Kubernetes RBAC.
            type: bool
        identity_type:
            description: Type of managed identity for the cluster.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMAksCluster(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            dns_prefix=dict(
                type='str',
            ),
            kubernetes_version=dict(
                type='str',
            ),
            node_resource_group=dict(
                type='str',
            ),
            network_plugin=dict(
                type='str',
                choices=['azure', 'kubenet', 'none'],
            ),
            network_policy=dict(
                type='str',
                choices=['azure', 'calico'],
            ),
            service_cidr=dict(
                type='str',
            ),
            dns_service_ip=dict(
                type='str',
            ),
            default_node_pool_name=dict(
                type='str',
                default='default',
            ),
            default_node_pool_vm_size=dict(
                type='str',
            ),
            default_node_pool_count=dict(
                type='int',
            ),
            default_node_pool_min_count=dict(
                type='int',
            ),
            default_node_pool_max_count=dict(
                type='int',
            ),
            enable_rbac=dict(
                type='bool',
                default=True,
            ),
            identity_type=dict(
                type='str',
                choices=['SystemAssigned', 'UserAssigned', 'None'],
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.dns_prefix = None
        self.kubernetes_version = None
        self.node_resource_group = None
        self.network_plugin = None
        self.network_policy = None
        self.service_cidr = None
        self.dns_service_ip = None
        self.default_node_pool_name = None
        self.default_node_pool_vm_size = None
        self.default_node_pool_count = None
        self.default_node_pool_min_count = None
        self.default_node_pool_max_count = None
        self.enable_rbac = None
        self.identity_type = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.dns_prefix is not None:
            body['dnsPrefix'] = self.dns_prefix
        if self.kubernetes_version is not None:
            body['kubernetesVersion'] = self.kubernetes_version
        if self.node_resource_group is not None:
            body['nodeResourceGroup'] = self.node_resource_group
        if self.network_plugin is not None:
            _target = body
            _target = _target.setdefault('networkProfile', {})
            _target['networkPlugin'] = self.network_plugin
        if self.network_policy is not None:
            _target = body
            _target = _target.setdefault('networkProfile', {})
            _target['networkPolicy'] = self.network_policy
        if self.service_cidr is not None:
            _target = body
            _target = _target.setdefault('networkProfile', {})
            _target['serviceCidr'] = self.service_cidr
        if self.dns_service_ip is not None:
            _target = body
            _target = _target.setdefault('networkProfile', {})
            _target['dnsServiceIP'] = self.dns_service_ip
        if self.default_node_pool_name is not None:
            _target = body
            _target = _target.setdefault('agentPoolProfiles[0]', {})
            _target['name'] = self.default_node_pool_name
        if self.default_node_pool_vm_size is not None:
            _target = body
            _target = _target.setdefault('agentPoolProfiles[0]', {})
            _target['vmSize'] = self.default_node_pool_vm_size
        if self.default_node_pool_count is not None:
            _target = body
            _target = _target.setdefault('agentPoolProfiles[0]', {})
            _target['count'] = self.default_node_pool_count
        if self.default_node_pool_min_count is not None:
            _target = body
            _target = _target.setdefault('agentPoolProfiles[0]', {})
            _target['minCount'] = self.default_node_pool_min_count
        if self.default_node_pool_max_count is not None:
            _target = body
            _target = _target.setdefault('agentPoolProfiles[0]', {})
            _target['maxCount'] = self.default_node_pool_max_count
        if self.enable_rbac is not None:
            body['enableRBAC'] = self.enable_rbac
        if self.identity_type is not None:
            _target = body
            _target = _target.setdefault('identity', {})
            _target['type'] = self.identity_type

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check dns_prefix
        if self.dns_prefix is not None:
            if existing.get('dnsPrefix') != self.dns_prefix:
                return True
        # Check kubernetes_version
        if self.kubernetes_version is not None:
            if existing.get('kubernetesVersion') != self.kubernetes_version:
                return True
        # Check node_resource_group
        if self.node_resource_group is not None:
            if existing.get('nodeResourceGroup') != self.node_resource_group:
                return True
        # Check network_plugin
        if self.network_plugin is not None:
            _existing = existing
            _existing = _existing.get('networkProfile', {}) if isinstance(_existing, dict) else {}
            if _existing.get('networkPlugin') != self.network_plugin:
                return True
        # Check network_policy
        if self.network_policy is not None:
            _existing = existing
            _existing = _existing.get('networkProfile', {}) if isinstance(_existing, dict) else {}
            if _existing.get('networkPolicy') != self.network_policy:
                return True
        # Check service_cidr
        if self.service_cidr is not None:
            _existing = existing
            _existing = _existing.get('networkProfile', {}) if isinstance(_existing, dict) else {}
            if _existing.get('serviceCidr') != self.service_cidr:
                return True
        # Check dns_service_ip
        if self.dns_service_ip is not None:
            _existing = existing
            _existing = _existing.get('networkProfile', {}) if isinstance(_existing, dict) else {}
            if _existing.get('dnsServiceIP') != self.dns_service_ip:
                return True
        # Check default_node_pool_name
        if self.default_node_pool_name is not None:
            _existing = existing
            _existing = _existing.get('agentPoolProfiles[0]', {}) if isinstance(_existing, dict) else {}
            if _existing.get('name') != self.default_node_pool_name:
                return True
        # Check default_node_pool_vm_size
        if self.default_node_pool_vm_size is not None:
            _existing = existing
            _existing = _existing.get('agentPoolProfiles[0]', {}) if isinstance(_existing, dict) else {}
            if _existing.get('vmSize') != self.default_node_pool_vm_size:
                return True
        # Check default_node_pool_count
        if self.default_node_pool_count is not None:
            _existing = existing
            _existing = _existing.get('agentPoolProfiles[0]', {}) if isinstance(_existing, dict) else {}
            if _existing.get('count') != self.default_node_pool_count:
                return True
        # Check default_node_pool_min_count
        if self.default_node_pool_min_count is not None:
            _existing = existing
            _existing = _existing.get('agentPoolProfiles[0]', {}) if isinstance(_existing, dict) else {}
            if _existing.get('minCount') != self.default_node_pool_min_count:
                return True
        # Check default_node_pool_max_count
        if self.default_node_pool_max_count is not None:
            _existing = existing
            _existing = _existing.get('agentPoolProfiles[0]', {}) if isinstance(_existing, dict) else {}
            if _existing.get('maxCount') != self.default_node_pool_max_count:
                return True
        # Check enable_rbac
        if self.enable_rbac is not None:
            if existing.get('enableRBAC') != self.enable_rbac:
                return True
        # Check identity_type
        if self.identity_type is not None:
            _existing = existing
            _existing = _existing.get('identity', {}) if isinstance(_existing, dict) else {}
            if _existing.get('type') != self.identity_type:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2024-02-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2024-02-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2024-02-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.ContainerService/managedClusters/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['dns_prefix'] = response.get('dnsPrefix')
        result['kubernetes_version'] = response.get('kubernetesVersion')
        result['node_resource_group'] = response.get('nodeResourceGroup')
        _body = response
        _body = _body.get('networkProfile', {}) if isinstance(_body, dict) else {}
        result['network_plugin'] = _body.get('networkPlugin')
        _body = response
        _body = _body.get('networkProfile', {}) if isinstance(_body, dict) else {}
        result['network_policy'] = _body.get('networkPolicy')
        _body = response
        _body = _body.get('networkProfile', {}) if isinstance(_body, dict) else {}
        result['service_cidr'] = _body.get('serviceCidr')
        _body = response
        _body = _body.get('networkProfile', {}) if isinstance(_body, dict) else {}
        result['dns_service_ip'] = _body.get('dnsServiceIP')
        _body = response
        _body = _body.get('agentPoolProfiles[0]', {}) if isinstance(_body, dict) else {}
        result['default_node_pool_name'] = _body.get('name')
        _body = response
        _body = _body.get('agentPoolProfiles[0]', {}) if isinstance(_body, dict) else {}
        result['default_node_pool_vm_size'] = _body.get('vmSize')
        _body = response
        _body = _body.get('agentPoolProfiles[0]', {}) if isinstance(_body, dict) else {}
        result['default_node_pool_count'] = _body.get('count')
        _body = response
        _body = _body.get('agentPoolProfiles[0]', {}) if isinstance(_body, dict) else {}
        result['default_node_pool_min_count'] = _body.get('minCount')
        _body = response
        _body = _body.get('agentPoolProfiles[0]', {}) if isinstance(_body, dict) else {}
        result['default_node_pool_max_count'] = _body.get('maxCount')
        result['enable_rbac'] = response.get('enableRBAC')
        _body = response
        _body = _body.get('identity', {}) if isinstance(_body, dict) else {}
        result['identity_type'] = _body.get('type')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMAksCluster()


if __name__ == '__main__':
    main()
