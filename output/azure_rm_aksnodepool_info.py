#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_aksnodepool_info
version_added: "0.1.0"
short_description: Get facts for Azure AksNodePool
description:
    - Get facts for a specific AksNodePool or list all AksNodePools in a resource group.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the aksnodepool.
            - If not specified, all aksnodepools in the resource group will be listed.
        type: str
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure AksNodePool REST API
      link: https://learn.microsoft.com/en-us/azure/aks/create-node-pools
'''

EXAMPLES = r'''
- name: Get facts for a specific AksNodePool
  azure_rm_aksnodepool_info:
    resource_group: myResourceGroup
    name: myAksNodePool

- name: List all AksNodePools in a resource group
  azure_rm_aksnodepool_info:
    resource_group: myResourceGroup
'''

RETURN = r'''
aksnodepools:
    description:
        - List of aksnodepool dicts.
    returned: always
    type: list
    elements: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        cluster_name:
            description: Name of the AKS cluster.
            type: str
        vm_size:
            description: VM size for the node pool agents.
            type: str
        count:
            description: Number of nodes in the node pool.
            type: int
        min_count:
            description: Minimum number of nodes for autoscaling.
            type: int
        max_count:
            description: Maximum number of nodes for autoscaling.
            type: int
        enable_auto_scaling:
            description: Whether to enable node pool autoscaling.
            type: bool
        os_type:
            description: Operating system type for the node pool.
            type: str
        os_sku:
            description: OS SKU for the node pool.
            type: str
        mode:
            description: Mode of the node pool (System or User).
            type: str
        max_pods:
            description: Maximum number of pods per node.
            type: int
        availability_zones:
            description: List of availability zones for the node pool.
            type: list
        node_labels:
            description: Kubernetes labels applied to nodes in the pool.
            type: dict
        node_taints:
            description: Kubernetes taints applied to nodes in the pool.
            type: list
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMAksNodePoolInfo(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str'),
        )

        self.resource_group = None
        self.name = None
        self.tags = None

        self.results = dict(
            changed=False,
            aksnodepools=[],
        )

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=False,
            facts_module=True,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        result_key = 'aksnodepools'

        if self.name:
            response = self.get_resource()
            if response:
                if self.has_tags(response.get('tags'), self.tags):
                    self.results[result_key] = [self.format_response(response)]
        else:
            responses = self.list_by_resource_group()
            for item in responses:
                if self.has_tags(item.get('tags'), self.tags):
                    self.results[result_key].append(self.format_response(item))

        return self.results

    def get_resource(self):
        try:
            url = (
                '/subscriptions/{subscription_id}'
                '/resourceGroups/{resource_group}'
                '/providers/Microsoft.ContainerService/managedClusters/agentPools/{name}'
            ).format(
                subscription_id=self.subscription_id,
                resource_group=self.resource_group,
                name=self.name,
            )
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2024-02-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def list_by_resource_group(self):
        try:
            url = (
                '/subscriptions/{subscription_id}'
                '/resourceGroups/{resource_group}'
                '/providers/Microsoft.ContainerService/managedClusters/agentPools'
            ).format(
                subscription_id=self.subscription_id,
                resource_group=self.resource_group,
            )
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2024-02-01'},
                None, None, [200], 0, 0,
            )
            result = self.deserialize_response(response)
            if result and isinstance(result, dict):
                return result.get('value', [])
            return []
        except Exception:
            return []

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['cluster_name'] = response.get('managedClusterName')
        result['vm_size'] = response.get('vmSize')
        result['count'] = response.get('count')
        result['min_count'] = response.get('minCount')
        result['max_count'] = response.get('maxCount')
        result['enable_auto_scaling'] = response.get('enableAutoScaling')
        result['os_type'] = response.get('osType')
        result['os_sku'] = response.get('osSKU')
        result['mode'] = response.get('mode')
        result['max_pods'] = response.get('maxPods')
        result['availability_zones'] = response.get('availabilityZones')
        result['node_labels'] = response.get('nodeLabels')
        result['node_taints'] = response.get('nodeTaints')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMAksNodePoolInfo()


if __name__ == '__main__':
    main()
