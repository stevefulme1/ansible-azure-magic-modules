#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_webapp
version_added: "0.1.0"
short_description: Manage Azure Web App instances.
description:
    - Create, update, or delete an Azure WebApp.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the webapp.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    server_farm_id:
        description:
            - Resource ID of the App Service Plan.
        type: str
    kind:
        description:
            - Kind of the Web App.
        type: str
        choices:
            - "app"
            - "app,linux"
            - "functionapp"
            - "functionapp,linux"
    enabled:
        description:
            - Whether the Web App is enabled.
        type: bool
        default: "True"
    https_only:
        description:
            - Whether the Web App only accepts HTTPS traffic.
        type: bool
        default: "True"
    client_affinity_enabled:
        description:
            - Whether client affinity is enabled.
        type: bool
    runtime_stack:
        description:
            - Runtime stack for the Web App.
        type: str
    runtime_version:
        description:
            - Runtime version for the Web App.
        type: str
    always_on:
        description:
            - Whether the Web App is always on.
        type: bool
    state:
        description:
            - Assert the state of the webapp.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure WebApp REST API
      link: https://learn.microsoft.com/en-us/azure/app-service/overview
'''

EXAMPLES = r'''
- name: Create WebApp
  azure_rm_webapp:
    resource_group: myResourceGroup
    name: myWebApp
    location: eastus
    state: present

- name: Delete WebApp
  azure_rm_webapp:
    resource_group: myResourceGroup
    name: myWebApp
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the webapp.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        server_farm_id:
            description: Resource ID of the App Service Plan.
            type: str
        kind:
            description: Kind of the Web App.
            type: str
        enabled:
            description: Whether the Web App is enabled.
            type: bool
        https_only:
            description: Whether the Web App only accepts HTTPS traffic.
            type: bool
        client_affinity_enabled:
            description: Whether client affinity is enabled.
            type: bool
        runtime_stack:
            description: Runtime stack for the Web App.
            type: str
        runtime_version:
            description: Runtime version for the Web App.
            type: str
        always_on:
            description: Whether the Web App is always on.
            type: bool
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMWebApp(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            server_farm_id=dict(
                type='str',
            ),
            kind=dict(
                type='str',
                choices=['app', 'app,linux', 'functionapp', 'functionapp,linux'],
            ),
            enabled=dict(
                type='bool',
                default=True,
            ),
            https_only=dict(
                type='bool',
                default=True,
            ),
            client_affinity_enabled=dict(
                type='bool',
            ),
            runtime_stack=dict(
                type='str',
            ),
            runtime_version=dict(
                type='str',
            ),
            always_on=dict(
                type='bool',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.server_farm_id = None
        self.kind = None
        self.enabled = None
        self.https_only = None
        self.client_affinity_enabled = None
        self.runtime_stack = None
        self.runtime_version = None
        self.always_on = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.server_farm_id is not None:
            body['serverFarmId'] = self.server_farm_id
        if self.kind is not None:
            body['kind'] = self.kind
        if self.enabled is not None:
            body['enabled'] = self.enabled
        if self.https_only is not None:
            body['httpsOnly'] = self.https_only
        if self.client_affinity_enabled is not None:
            body['clientAffinityEnabled'] = self.client_affinity_enabled
        if self.runtime_stack is not None:
            _target = body
            _target = _target.setdefault('siteConfig', {})
            _target['linuxFxVersion'] = self.runtime_stack
        if self.runtime_version is not None:
            _target = body
            _target = _target.setdefault('siteConfig', {})
            _target['netFrameworkVersion'] = self.runtime_version
        if self.always_on is not None:
            _target = body
            _target = _target.setdefault('siteConfig', {})
            _target['alwaysOn'] = self.always_on

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check server_farm_id
        if self.server_farm_id is not None:
            if existing.get('serverFarmId') != self.server_farm_id:
                return True
        # Check kind
        if self.kind is not None:
            if existing.get('kind') != self.kind:
                return True
        # Check enabled
        if self.enabled is not None:
            if existing.get('enabled') != self.enabled:
                return True
        # Check https_only
        if self.https_only is not None:
            if existing.get('httpsOnly') != self.https_only:
                return True
        # Check client_affinity_enabled
        if self.client_affinity_enabled is not None:
            if existing.get('clientAffinityEnabled') != self.client_affinity_enabled:
                return True
        # Check runtime_stack
        if self.runtime_stack is not None:
            _existing = existing
            _existing = _existing.get('siteConfig', {}) if isinstance(_existing, dict) else {}
            if _existing.get('linuxFxVersion') != self.runtime_stack:
                return True
        # Check runtime_version
        if self.runtime_version is not None:
            _existing = existing
            _existing = _existing.get('siteConfig', {}) if isinstance(_existing, dict) else {}
            if _existing.get('netFrameworkVersion') != self.runtime_version:
                return True
        # Check always_on
        if self.always_on is not None:
            _existing = existing
            _existing = _existing.get('siteConfig', {}) if isinstance(_existing, dict) else {}
            if _existing.get('alwaysOn') != self.always_on:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2023-12-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2023-12-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2023-12-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Web/sites/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['server_farm_id'] = response.get('serverFarmId')
        result['kind'] = response.get('kind')
        result['enabled'] = response.get('enabled')
        result['https_only'] = response.get('httpsOnly')
        result['client_affinity_enabled'] = response.get('clientAffinityEnabled')
        _body = response
        _body = _body.get('siteConfig', {}) if isinstance(_body, dict) else {}
        result['runtime_stack'] = _body.get('linuxFxVersion')
        _body = response
        _body = _body.get('siteConfig', {}) if isinstance(_body, dict) else {}
        result['runtime_version'] = _body.get('netFrameworkVersion')
        _body = response
        _body = _body.get('siteConfig', {}) if isinstance(_body, dict) else {}
        result['always_on'] = _body.get('alwaysOn')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMWebApp()


if __name__ == '__main__':
    main()
