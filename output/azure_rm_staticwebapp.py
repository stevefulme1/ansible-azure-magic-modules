#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_staticwebapp
version_added: "0.1.0"
short_description: Manage Azure Static Web App instances.
description:
    - Create, update, or delete an Azure StaticWebApp.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the staticwebapp.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    sku_name:
        description:
            - SKU name of the Static Web App.
        type: str
        choices:
            - "Free"
            - "Standard"
    repository_url:
        description:
            - URL of the source code repository.
        type: str
    branch:
        description:
            - Branch name for the repository.
        type: str
    repository_token:
        description:
            - Token for accessing the repository.
        type: str
    build_app_location:
        description:
            - Path to the app source code relative to repo root.
        type: str
    build_api_location:
        description:
            - Path to the API source code relative to repo root.
        type: str
    build_output_location:
        description:
            - Path to the build output relative to app location.
        type: str
    state:
        description:
            - Assert the state of the staticwebapp.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure StaticWebApp REST API
      link: https://learn.microsoft.com/en-us/azure/static-web-apps/overview
'''

EXAMPLES = r'''
- name: Create StaticWebApp
  azure_rm_staticwebapp:
    resource_group: myResourceGroup
    name: myStaticWebApp
    location: eastus
    state: present

- name: Delete StaticWebApp
  azure_rm_staticwebapp:
    resource_group: myResourceGroup
    name: myStaticWebApp
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the staticwebapp.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        sku_name:
            description: SKU name of the Static Web App.
            type: str
        repository_url:
            description: URL of the source code repository.
            type: str
        branch:
            description: Branch name for the repository.
            type: str
        repository_token:
            description: Token for accessing the repository.
            type: str
        build_app_location:
            description: Path to the app source code relative to repo root.
            type: str
        build_api_location:
            description: Path to the API source code relative to repo root.
            type: str
        build_output_location:
            description: Path to the build output relative to app location.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMStaticWebApp(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            sku_name=dict(
                type='str',
                choices=['Free', 'Standard'],
            ),
            repository_url=dict(
                type='str',
            ),
            branch=dict(
                type='str',
            ),
            repository_token=dict(
                type='str',
            ),
            build_app_location=dict(
                type='str',
            ),
            build_api_location=dict(
                type='str',
            ),
            build_output_location=dict(
                type='str',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.sku_name = None
        self.repository_url = None
        self.branch = None
        self.repository_token = None
        self.build_app_location = None
        self.build_api_location = None
        self.build_output_location = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.sku_name is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['name'] = self.sku_name
        if self.repository_url is not None:
            body['repositoryUrl'] = self.repository_url
        if self.branch is not None:
            body['branch'] = self.branch
        if self.repository_token is not None:
            body['repositoryToken'] = self.repository_token
        if self.build_app_location is not None:
            _target = body
            _target = _target.setdefault('buildProperties', {})
            _target['appLocation'] = self.build_app_location
        if self.build_api_location is not None:
            _target = body
            _target = _target.setdefault('buildProperties', {})
            _target['apiLocation'] = self.build_api_location
        if self.build_output_location is not None:
            _target = body
            _target = _target.setdefault('buildProperties', {})
            _target['outputLocation'] = self.build_output_location

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check sku_name
        if self.sku_name is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('name') != self.sku_name:
                return True
        # Check repository_url
        if self.repository_url is not None:
            if existing.get('repositoryUrl') != self.repository_url:
                return True
        # Check branch
        if self.branch is not None:
            if existing.get('branch') != self.branch:
                return True
        # Check repository_token
        if self.repository_token is not None:
            if existing.get('repositoryToken') != self.repository_token:
                return True
        # Check build_app_location
        if self.build_app_location is not None:
            _existing = existing
            _existing = _existing.get('buildProperties', {}) if isinstance(_existing, dict) else {}
            if _existing.get('appLocation') != self.build_app_location:
                return True
        # Check build_api_location
        if self.build_api_location is not None:
            _existing = existing
            _existing = _existing.get('buildProperties', {}) if isinstance(_existing, dict) else {}
            if _existing.get('apiLocation') != self.build_api_location:
                return True
        # Check build_output_location
        if self.build_output_location is not None:
            _existing = existing
            _existing = _existing.get('buildProperties', {}) if isinstance(_existing, dict) else {}
            if _existing.get('outputLocation') != self.build_output_location:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2023-12-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2023-12-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2023-12-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Web/staticSites/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_name'] = _body.get('name')
        result['repository_url'] = response.get('repositoryUrl')
        result['branch'] = response.get('branch')
        result['repository_token'] = response.get('repositoryToken')
        _body = response
        _body = _body.get('buildProperties', {}) if isinstance(_body, dict) else {}
        result['build_app_location'] = _body.get('appLocation')
        _body = response
        _body = _body.get('buildProperties', {}) if isinstance(_body, dict) else {}
        result['build_api_location'] = _body.get('apiLocation')
        _body = response
        _body = _body.get('buildProperties', {}) if isinstance(_body, dict) else {}
        result['build_output_location'] = _body.get('outputLocation')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMStaticWebApp()


if __name__ == '__main__':
    main()
