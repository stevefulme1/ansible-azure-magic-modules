#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_policydefinition
version_added: "0.1.0"
short_description: Manage Azure Policy definitions.
description:
    - Create, update, or delete an Azure PolicyDefinition.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the policydefinition.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    display_name:
        description:
            - The display name of the policy definition.
        type: str
    description:
        description:
            - Description of the policy definition.
        type: str
    policy_type:
        description:
            - The type of policy definition.
        type: str
        choices:
            - "BuiltIn"
            - "Custom"
            - "NotSpecified"
            - "Static"
    mode:
        description:
            - The policy definition mode.
        type: str
        choices:
            - "All"
            - "Indexed"
            - "Microsoft.KeyVault.Data"
    policy_rule:
        description:
            - The policy rule JSON object.
        type: dict
    parameters:
        description:
            - Parameter definitions for the policy.
        type: dict
    metadata:
        description:
            - Metadata for the policy definition.
        type: dict
    state:
        description:
            - Assert the state of the policydefinition.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure PolicyDefinition REST API
      link: https://learn.microsoft.com/en-us/azure/governance/policy/
'''

EXAMPLES = r'''
- name: Create PolicyDefinition
  azure_rm_policydefinition:
    resource_group: myResourceGroup
    name: myPolicyDefinition
    location: eastus
    state: present

- name: Delete PolicyDefinition
  azure_rm_policydefinition:
    resource_group: myResourceGroup
    name: myPolicyDefinition
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the policydefinition.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        display_name:
            description: The display name of the policy definition.
            type: str
        description:
            description: Description of the policy definition.
            type: str
        policy_type:
            description: The type of policy definition.
            type: str
        mode:
            description: The policy definition mode.
            type: str
        policy_rule:
            description: The policy rule JSON object.
            type: dict
        parameters:
            description: Parameter definitions for the policy.
            type: dict
        metadata:
            description: Metadata for the policy definition.
            type: dict
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMPolicyDefinition(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            display_name=dict(
                type='str',
            ),
            description=dict(
                type='str',
            ),
            policy_type=dict(
                type='str',
                choices=['BuiltIn', 'Custom', 'NotSpecified', 'Static'],
            ),
            mode=dict(
                type='str',
                choices=['All', 'Indexed', 'Microsoft.KeyVault.Data'],
            ),
            policy_rule=dict(
                type='dict',
            ),
            parameters=dict(
                type='dict',
            ),
            metadata=dict(
                type='dict',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.display_name = None
        self.description = None
        self.policy_type = None
        self.mode = None
        self.policy_rule = None
        self.parameters = None
        self.metadata = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )

        properties = body.setdefault('properties', {})
        if self.display_name is not None:
            properties['displayName'] = self.display_name
        if self.description is not None:
            properties['description'] = self.description
        if self.policy_type is not None:
            properties['policyType'] = self.policy_type
        if self.mode is not None:
            properties['mode'] = self.mode
        if self.policy_rule is not None:
            properties['policyRule'] = self.policy_rule
        if self.parameters is not None:
            properties['parameters'] = self.parameters
        if self.metadata is not None:
            properties['metadata'] = self.metadata

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check display_name
        if self.display_name is not None:
            if existing.get('properties', {}).get('displayName') != self.display_name:
                return True
        # Check description
        if self.description is not None:
            if existing.get('properties', {}).get('description') != self.description:
                return True
        # Check policy_type
        if self.policy_type is not None:
            if existing.get('properties', {}).get('policyType') != self.policy_type:
                return True
        # Check mode
        if self.mode is not None:
            if existing.get('properties', {}).get('mode') != self.mode:
                return True
        # Check policy_rule
        if self.policy_rule is not None:
            if existing.get('properties', {}).get('policyRule') != self.policy_rule:
                return True
        # Check parameters
        if self.parameters is not None:
            if existing.get('properties', {}).get('parameters') != self.parameters:
                return True
        # Check metadata
        if self.metadata is not None:
            if existing.get('properties', {}).get('metadata') != self.metadata:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2023-04-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2023-04-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2023-04-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Authorization/policyDefinitions/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['display_name'] = response.get('properties', {}).get('displayName')
        result['description'] = response.get('properties', {}).get('description')
        result['policy_type'] = response.get('properties', {}).get('policyType')
        result['mode'] = response.get('properties', {}).get('mode')
        result['policy_rule'] = response.get('properties', {}).get('policyRule')
        result['parameters'] = response.get('properties', {}).get('parameters')
        result['metadata'] = response.get('properties', {}).get('metadata')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMPolicyDefinition()


if __name__ == '__main__':
    main()
