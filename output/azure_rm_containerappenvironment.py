#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_containerappenvironment
version_added: "0.1.0"
short_description: Manage Azure Container App Environment instances.
description:
    - Create, update, or delete an Azure ContainerAppEnvironment.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the containerappenvironment.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    log_analytics_workspace_customer_id:
        description:
            - Log Analytics workspace customer ID.
        type: str
    log_analytics_workspace_shared_key:
        description:
            - Log Analytics workspace shared key.
        type: str
    dapr_ai_instrumentation_key:
        description:
            - Dapr Application Insights instrumentation key.
        type: str
    zone_redundant:
        description:
            - Whether zone redundancy is enabled.
        type: bool
    internal_load_balancer_enabled:
        description:
            - Whether internal load balancer is enabled.
        type: bool
    infrastructure_subnet_id:
        description:
            - Resource ID of the infrastructure subnet.
        type: str
    state:
        description:
            - Assert the state of the containerappenvironment.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure ContainerAppEnvironment REST API
      link: https://learn.microsoft.com/en-us/azure/container-apps/environment
'''

EXAMPLES = r'''
- name: Create ContainerAppEnvironment
  azure_rm_containerappenvironment:
    resource_group: myResourceGroup
    name: myContainerAppEnvironment
    location: eastus
    state: present

- name: Delete ContainerAppEnvironment
  azure_rm_containerappenvironment:
    resource_group: myResourceGroup
    name: myContainerAppEnvironment
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the containerappenvironment.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        log_analytics_workspace_customer_id:
            description: Log Analytics workspace customer ID.
            type: str
        log_analytics_workspace_shared_key:
            description: Log Analytics workspace shared key.
            type: str
        dapr_ai_instrumentation_key:
            description: Dapr Application Insights instrumentation key.
            type: str
        zone_redundant:
            description: Whether zone redundancy is enabled.
            type: bool
        internal_load_balancer_enabled:
            description: Whether internal load balancer is enabled.
            type: bool
        infrastructure_subnet_id:
            description: Resource ID of the infrastructure subnet.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMContainerAppEnvironment(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            log_analytics_workspace_customer_id=dict(
                type='str',
            ),
            log_analytics_workspace_shared_key=dict(
                type='str',
            ),
            dapr_ai_instrumentation_key=dict(
                type='str',
            ),
            zone_redundant=dict(
                type='bool',
            ),
            internal_load_balancer_enabled=dict(
                type='bool',
            ),
            infrastructure_subnet_id=dict(
                type='str',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.log_analytics_workspace_customer_id = None
        self.log_analytics_workspace_shared_key = None
        self.dapr_ai_instrumentation_key = None
        self.zone_redundant = None
        self.internal_load_balancer_enabled = None
        self.infrastructure_subnet_id = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.log_analytics_workspace_customer_id is not None:
            _target = body
            _target = _target.setdefault('appLogsConfiguration', {})
            _target = _target.setdefault('logAnalyticsConfiguration', {})
            _target['customerId'] = self.log_analytics_workspace_customer_id
        if self.log_analytics_workspace_shared_key is not None:
            _target = body
            _target = _target.setdefault('appLogsConfiguration', {})
            _target = _target.setdefault('logAnalyticsConfiguration', {})
            _target['sharedKey'] = self.log_analytics_workspace_shared_key
        if self.dapr_ai_instrumentation_key is not None:
            body['daprAIInstrumentationKey'] = self.dapr_ai_instrumentation_key
        if self.zone_redundant is not None:
            body['zoneRedundant'] = self.zone_redundant
        if self.internal_load_balancer_enabled is not None:
            _target = body
            _target = _target.setdefault('vnetConfiguration', {})
            _target['internal'] = self.internal_load_balancer_enabled
        if self.infrastructure_subnet_id is not None:
            _target = body
            _target = _target.setdefault('vnetConfiguration', {})
            _target['infrastructureSubnetId'] = self.infrastructure_subnet_id

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check log_analytics_workspace_customer_id
        if self.log_analytics_workspace_customer_id is not None:
            _existing = existing
            _existing = _existing.get('appLogsConfiguration', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('logAnalyticsConfiguration', {}) if isinstance(_existing, dict) else {}
            if _existing.get('customerId') != self.log_analytics_workspace_customer_id:
                return True
        # Check log_analytics_workspace_shared_key
        if self.log_analytics_workspace_shared_key is not None:
            _existing = existing
            _existing = _existing.get('appLogsConfiguration', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('logAnalyticsConfiguration', {}) if isinstance(_existing, dict) else {}
            if _existing.get('sharedKey') != self.log_analytics_workspace_shared_key:
                return True
        # Check dapr_ai_instrumentation_key
        if self.dapr_ai_instrumentation_key is not None:
            if existing.get('daprAIInstrumentationKey') != self.dapr_ai_instrumentation_key:
                return True
        # Check zone_redundant
        if self.zone_redundant is not None:
            if existing.get('zoneRedundant') != self.zone_redundant:
                return True
        # Check internal_load_balancer_enabled
        if self.internal_load_balancer_enabled is not None:
            _existing = existing
            _existing = _existing.get('vnetConfiguration', {}) if isinstance(_existing, dict) else {}
            if _existing.get('internal') != self.internal_load_balancer_enabled:
                return True
        # Check infrastructure_subnet_id
        if self.infrastructure_subnet_id is not None:
            _existing = existing
            _existing = _existing.get('vnetConfiguration', {}) if isinstance(_existing, dict) else {}
            if _existing.get('infrastructureSubnetId') != self.infrastructure_subnet_id:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2024-03-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2024-03-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2024-03-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.App/managedEnvironments/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        _body = response
        _body = _body.get('appLogsConfiguration', {}) if isinstance(_body, dict) else {}
        _body = _body.get('logAnalyticsConfiguration', {}) if isinstance(_body, dict) else {}
        result['log_analytics_workspace_customer_id'] = _body.get('customerId')
        _body = response
        _body = _body.get('appLogsConfiguration', {}) if isinstance(_body, dict) else {}
        _body = _body.get('logAnalyticsConfiguration', {}) if isinstance(_body, dict) else {}
        result['log_analytics_workspace_shared_key'] = _body.get('sharedKey')
        result['dapr_ai_instrumentation_key'] = response.get('daprAIInstrumentationKey')
        result['zone_redundant'] = response.get('zoneRedundant')
        _body = response
        _body = _body.get('vnetConfiguration', {}) if isinstance(_body, dict) else {}
        result['internal_load_balancer_enabled'] = _body.get('internal')
        _body = response
        _body = _body.get('vnetConfiguration', {}) if isinstance(_body, dict) else {}
        result['infrastructure_subnet_id'] = _body.get('infrastructureSubnetId')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMContainerAppEnvironment()


if __name__ == '__main__':
    main()
