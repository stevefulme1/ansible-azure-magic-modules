#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_monitormetricalert
version_added: "0.1.0"
short_description: Manage Azure Monitor metric alert rules.
description:
    - Create, update, or delete an Azure MonitorMetricAlert.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the monitormetricalert.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    description:
        description:
            - Description of the metric alert.
        type: str
    severity:
        description:
            - Alert severity level.
        type: int
        required: true
        choices:
            - "0"
            - "1"
            - "2"
            - "3"
            - "4"
    enabled:
        description:
            - Whether the metric alert is enabled.
        type: bool
        default: "True"
    scopes:
        description:
            - List of resource IDs this alert monitors.
        type: list
        required: true
    evaluation_frequency:
        description:
            - How often the metric alert is evaluated (ISO 8601).
        type: str
        required: true
    window_size:
        description:
            - Period of time to aggregate metrics (ISO 8601).
        type: str
        required: true
    target_resource_type:
        description:
            - The resource type of the target resources.
        type: str
    target_resource_region:
        description:
            - The region of the target resources.
        type: str
    auto_mitigate:
        description:
            - Whether the alert should be auto-resolved.
        type: bool
        default: "True"
    state:
        description:
            - Assert the state of the monitormetricalert.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure MonitorMetricAlert REST API
      link: https://learn.microsoft.com/en-us/azure/azure-monitor/alerts/alerts-metric-overview
'''

EXAMPLES = r'''
- name: Create MonitorMetricAlert
  azure_rm_monitormetricalert:
    resource_group: myResourceGroup
    name: myMonitorMetricAlert
    location: eastus
    severity: 1
    scopes:
      - "example_item"
    evaluation_frequency: "example_value"
    window_size: "example_value"
    state: present

- name: Delete MonitorMetricAlert
  azure_rm_monitormetricalert:
    resource_group: myResourceGroup
    name: myMonitorMetricAlert
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the monitormetricalert.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        description:
            description: Description of the metric alert.
            type: str
        severity:
            description: Alert severity level.
            type: int
        enabled:
            description: Whether the metric alert is enabled.
            type: bool
        scopes:
            description: List of resource IDs this alert monitors.
            type: list
        evaluation_frequency:
            description: How often the metric alert is evaluated (ISO 8601).
            type: str
        window_size:
            description: Period of time to aggregate metrics (ISO 8601).
            type: str
        target_resource_type:
            description: The resource type of the target resources.
            type: str
        target_resource_region:
            description: The region of the target resources.
            type: str
        auto_mitigate:
            description: Whether the alert should be auto-resolved.
            type: bool
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMMonitorMetricAlert(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            description=dict(
                type='str',
            ),
            severity=dict(
                type='int',
                required=True,
                choices=[0, 1, 2, 3, 4],
            ),
            enabled=dict(
                type='bool',
                default=True,
            ),
            scopes=dict(
                type='list',
                required=True,
            ),
            evaluation_frequency=dict(
                type='str',
                required=True,
            ),
            window_size=dict(
                type='str',
                required=True,
            ),
            target_resource_type=dict(
                type='str',
            ),
            target_resource_region=dict(
                type='str',
            ),
            auto_mitigate=dict(
                type='bool',
                default=True,
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.description = None
        self.severity = None
        self.enabled = None
        self.scopes = None
        self.evaluation_frequency = None
        self.window_size = None
        self.target_resource_type = None
        self.target_resource_region = None
        self.auto_mitigate = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )

        properties = body.setdefault('properties', {})
        if self.description is not None:
            properties['description'] = self.description
        if self.severity is not None:
            properties['severity'] = self.severity
        if self.enabled is not None:
            properties['enabled'] = self.enabled
        if self.scopes is not None:
            properties['scopes'] = self.scopes
        if self.evaluation_frequency is not None:
            properties['evaluationFrequency'] = self.evaluation_frequency
        if self.window_size is not None:
            properties['windowSize'] = self.window_size
        if self.target_resource_type is not None:
            properties['targetResourceType'] = self.target_resource_type
        if self.target_resource_region is not None:
            properties['targetResourceRegion'] = self.target_resource_region
        if self.auto_mitigate is not None:
            properties['autoMitigate'] = self.auto_mitigate

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check description
        if self.description is not None:
            if existing.get('properties', {}).get('description') != self.description:
                return True
        # Check severity
        if self.severity is not None:
            if existing.get('properties', {}).get('severity') != self.severity:
                return True
        # Check enabled
        if self.enabled is not None:
            if existing.get('properties', {}).get('enabled') != self.enabled:
                return True
        # Check scopes
        if self.scopes is not None:
            if existing.get('properties', {}).get('scopes') != self.scopes:
                return True
        # Check evaluation_frequency
        if self.evaluation_frequency is not None:
            if existing.get('properties', {}).get('evaluationFrequency') != self.evaluation_frequency:
                return True
        # Check window_size
        if self.window_size is not None:
            if existing.get('properties', {}).get('windowSize') != self.window_size:
                return True
        # Check target_resource_type
        if self.target_resource_type is not None:
            if existing.get('properties', {}).get('targetResourceType') != self.target_resource_type:
                return True
        # Check target_resource_region
        if self.target_resource_region is not None:
            if existing.get('properties', {}).get('targetResourceRegion') != self.target_resource_region:
                return True
        # Check auto_mitigate
        if self.auto_mitigate is not None:
            if existing.get('properties', {}).get('autoMitigate') != self.auto_mitigate:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2018-03-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2018-03-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2018-03-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Insights/metricAlerts/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['description'] = response.get('properties', {}).get('description')
        result['severity'] = response.get('properties', {}).get('severity')
        result['enabled'] = response.get('properties', {}).get('enabled')
        result['scopes'] = response.get('properties', {}).get('scopes')
        result['evaluation_frequency'] = response.get('properties', {}).get('evaluationFrequency')
        result['window_size'] = response.get('properties', {}).get('windowSize')
        result['target_resource_type'] = response.get('properties', {}).get('targetResourceType')
        result['target_resource_region'] = response.get('properties', {}).get('targetResourceRegion')
        result['auto_mitigate'] = response.get('properties', {}).get('autoMitigate')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMMonitorMetricAlert()


if __name__ == '__main__':
    main()
