#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_iothub
version_added: "0.1.0"
short_description: Manage Azure IoT Hub instances.
description:
    - Create, update, or delete an Azure IoTHub.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the iothub.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    sku_name:
        description:
            - SKU name for the IoT Hub.
        type: str
        choices:
            - "F1"
            - "B1"
            - "B2"
            - "B3"
            - "S1"
            - "S2"
            - "S3"
    sku_capacity:
        description:
            - Number of provisioned IoT Hub units.
        type: int
    event_hub_retention_in_days:
        description:
            - Event Hub message retention in days.
        type: int
    event_hub_partition_count:
        description:
            - Number of Event Hub partitions.
        type: int
    cloud_to_device_max_delivery_count:
        description:
            - Max delivery count for cloud-to-device messages.
        type: int
    cloud_to_device_default_ttl:
        description:
            - Default TTL for cloud-to-device messages.
        type: str
    enable_file_upload_notifications:
        description:
            - Whether file upload notifications are enabled.
        type: bool
    state:
        description:
            - Assert the state of the iothub.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure IoTHub REST API
      link: https://learn.microsoft.com/en-us/azure/iot-hub/
'''

EXAMPLES = r'''
- name: Create IoTHub
  azure_rm_iothub:
    resource_group: myResourceGroup
    name: myIoTHub
    location: eastus
    state: present

- name: Delete IoTHub
  azure_rm_iothub:
    resource_group: myResourceGroup
    name: myIoTHub
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the iothub.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        sku_name:
            description: SKU name for the IoT Hub.
            type: str
        sku_capacity:
            description: Number of provisioned IoT Hub units.
            type: int
        event_hub_retention_in_days:
            description: Event Hub message retention in days.
            type: int
        event_hub_partition_count:
            description: Number of Event Hub partitions.
            type: int
        cloud_to_device_max_delivery_count:
            description: Max delivery count for cloud-to-device messages.
            type: int
        cloud_to_device_default_ttl:
            description: Default TTL for cloud-to-device messages.
            type: str
        enable_file_upload_notifications:
            description: Whether file upload notifications are enabled.
            type: bool
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMIoTHub(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            sku_name=dict(
                type='str',
                choices=['F1', 'B1', 'B2', 'B3', 'S1', 'S2', 'S3'],
            ),
            sku_capacity=dict(
                type='int',
            ),
            event_hub_retention_in_days=dict(
                type='int',
            ),
            event_hub_partition_count=dict(
                type='int',
            ),
            cloud_to_device_max_delivery_count=dict(
                type='int',
            ),
            cloud_to_device_default_ttl=dict(
                type='str',
            ),
            enable_file_upload_notifications=dict(
                type='bool',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.sku_name = None
        self.sku_capacity = None
        self.event_hub_retention_in_days = None
        self.event_hub_partition_count = None
        self.cloud_to_device_max_delivery_count = None
        self.cloud_to_device_default_ttl = None
        self.enable_file_upload_notifications = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.sku_name is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['name'] = self.sku_name
        if self.sku_capacity is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['capacity'] = self.sku_capacity
        if self.event_hub_retention_in_days is not None:
            _target = body
            _target = _target.setdefault('eventHubEndpoints', {})
            _target = _target.setdefault('events', {})
            _target['retentionTimeInDays'] = self.event_hub_retention_in_days
        if self.event_hub_partition_count is not None:
            _target = body
            _target = _target.setdefault('eventHubEndpoints', {})
            _target = _target.setdefault('events', {})
            _target['partitionCount'] = self.event_hub_partition_count
        if self.cloud_to_device_max_delivery_count is not None:
            _target = body
            _target = _target.setdefault('cloudToDevice', {})
            _target['maxDeliveryCount'] = self.cloud_to_device_max_delivery_count
        if self.cloud_to_device_default_ttl is not None:
            _target = body
            _target = _target.setdefault('cloudToDevice', {})
            _target['defaultTtlAsIso8601'] = self.cloud_to_device_default_ttl
        if self.enable_file_upload_notifications is not None:
            body['enableFileUploadNotifications'] = self.enable_file_upload_notifications

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check sku_name
        if self.sku_name is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('name') != self.sku_name:
                return True
        # Check sku_capacity
        if self.sku_capacity is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('capacity') != self.sku_capacity:
                return True
        # Check event_hub_retention_in_days
        if self.event_hub_retention_in_days is not None:
            _existing = existing
            _existing = _existing.get('eventHubEndpoints', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('events', {}) if isinstance(_existing, dict) else {}
            if _existing.get('retentionTimeInDays') != self.event_hub_retention_in_days:
                return True
        # Check event_hub_partition_count
        if self.event_hub_partition_count is not None:
            _existing = existing
            _existing = _existing.get('eventHubEndpoints', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('events', {}) if isinstance(_existing, dict) else {}
            if _existing.get('partitionCount') != self.event_hub_partition_count:
                return True
        # Check cloud_to_device_max_delivery_count
        if self.cloud_to_device_max_delivery_count is not None:
            _existing = existing
            _existing = _existing.get('cloudToDevice', {}) if isinstance(_existing, dict) else {}
            if _existing.get('maxDeliveryCount') != self.cloud_to_device_max_delivery_count:
                return True
        # Check cloud_to_device_default_ttl
        if self.cloud_to_device_default_ttl is not None:
            _existing = existing
            _existing = _existing.get('cloudToDevice', {}) if isinstance(_existing, dict) else {}
            if _existing.get('defaultTtlAsIso8601') != self.cloud_to_device_default_ttl:
                return True
        # Check enable_file_upload_notifications
        if self.enable_file_upload_notifications is not None:
            if existing.get('enableFileUploadNotifications') != self.enable_file_upload_notifications:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2021-07-02'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2021-07-02'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2021-07-02'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Devices/IotHubs/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_name'] = _body.get('name')
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_capacity'] = _body.get('capacity')
        _body = response
        _body = _body.get('eventHubEndpoints', {}) if isinstance(_body, dict) else {}
        _body = _body.get('events', {}) if isinstance(_body, dict) else {}
        result['event_hub_retention_in_days'] = _body.get('retentionTimeInDays')
        _body = response
        _body = _body.get('eventHubEndpoints', {}) if isinstance(_body, dict) else {}
        _body = _body.get('events', {}) if isinstance(_body, dict) else {}
        result['event_hub_partition_count'] = _body.get('partitionCount')
        _body = response
        _body = _body.get('cloudToDevice', {}) if isinstance(_body, dict) else {}
        result['cloud_to_device_max_delivery_count'] = _body.get('maxDeliveryCount')
        _body = response
        _body = _body.get('cloudToDevice', {}) if isinstance(_body, dict) else {}
        result['cloud_to_device_default_ttl'] = _body.get('defaultTtlAsIso8601')
        result['enable_file_upload_notifications'] = response.get('enableFileUploadNotifications')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMIoTHub()


if __name__ == '__main__':
    main()
