#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_cdnendpoint
version_added: "0.1.0"
short_description: Manage Azure CDN Endpoint instances.
description:
    - Create, update, or delete an Azure CdnEndpoint.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the cdnendpoint.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    profile_name:
        description:
            - Name of the CDN Profile.
        type: str
        required: true
    origin_host_name:
        description:
            - Hostname of the origin.
        type: str
    origin_path:
        description:
            - Path on the origin to retrieve content from.
        type: str
    is_http_allowed:
        description:
            - Whether HTTP traffic is allowed.
        type: bool
        default: "True"
    is_https_allowed:
        description:
            - Whether HTTPS traffic is allowed.
        type: bool
        default: "True"
    is_compression_enabled:
        description:
            - Whether content compression is enabled.
        type: bool
    content_types_to_compress:
        description:
            - List of content types to compress.
        type: list
    query_string_caching_behavior:
        description:
            - Query string caching behavior.
        type: str
        choices:
            - "IgnoreQueryString"
            - "BypassCaching"
            - "UseQueryString"
            - "NotSet"
    state:
        description:
            - Assert the state of the cdnendpoint.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure CdnEndpoint REST API
      link: https://learn.microsoft.com/en-us/azure/cdn/cdn-create-endpoint-how-to
'''

EXAMPLES = r'''
- name: Create CdnEndpoint
  azure_rm_cdnendpoint:
    resource_group: myResourceGroup
    name: myCdnEndpoint
    location: eastus
    profile_name: "example_value"
    state: present

- name: Delete CdnEndpoint
  azure_rm_cdnendpoint:
    resource_group: myResourceGroup
    name: myCdnEndpoint
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the cdnendpoint.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        profile_name:
            description: Name of the CDN Profile.
            type: str
        origin_host_name:
            description: Hostname of the origin.
            type: str
        origin_path:
            description: Path on the origin to retrieve content from.
            type: str
        is_http_allowed:
            description: Whether HTTP traffic is allowed.
            type: bool
        is_https_allowed:
            description: Whether HTTPS traffic is allowed.
            type: bool
        is_compression_enabled:
            description: Whether content compression is enabled.
            type: bool
        content_types_to_compress:
            description: List of content types to compress.
            type: list
        query_string_caching_behavior:
            description: Query string caching behavior.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMCdnEndpoint(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            profile_name=dict(
                type='str',
                required=True,
            ),
            origin_host_name=dict(
                type='str',
            ),
            origin_path=dict(
                type='str',
            ),
            is_http_allowed=dict(
                type='bool',
                default=True,
            ),
            is_https_allowed=dict(
                type='bool',
                default=True,
            ),
            is_compression_enabled=dict(
                type='bool',
            ),
            content_types_to_compress=dict(
                type='list',
            ),
            query_string_caching_behavior=dict(
                type='str',
                choices=['IgnoreQueryString', 'BypassCaching', 'UseQueryString', 'NotSet'],
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.profile_name = None
        self.origin_host_name = None
        self.origin_path = None
        self.is_http_allowed = None
        self.is_https_allowed = None
        self.is_compression_enabled = None
        self.content_types_to_compress = None
        self.query_string_caching_behavior = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.profile_name is not None:
            body['ProfileName'] = self.profile_name
        if self.origin_host_name is not None:
            _target = body
            _target = _target.setdefault('origins', {})
            _target['hostName'] = self.origin_host_name
        if self.origin_path is not None:
            body['originPath'] = self.origin_path
        if self.is_http_allowed is not None:
            body['isHttpAllowed'] = self.is_http_allowed
        if self.is_https_allowed is not None:
            body['isHttpsAllowed'] = self.is_https_allowed
        if self.is_compression_enabled is not None:
            body['isCompressionEnabled'] = self.is_compression_enabled
        if self.content_types_to_compress is not None:
            body['contentTypesToCompress'] = self.content_types_to_compress
        if self.query_string_caching_behavior is not None:
            body['queryStringCachingBehavior'] = self.query_string_caching_behavior

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check profile_name
        if self.profile_name is not None:
            if existing.get('ProfileName') != self.profile_name:
                return True
        # Check origin_host_name
        if self.origin_host_name is not None:
            _existing = existing
            _existing = _existing.get('origins', {}) if isinstance(_existing, dict) else {}
            if _existing.get('hostName') != self.origin_host_name:
                return True
        # Check origin_path
        if self.origin_path is not None:
            if existing.get('originPath') != self.origin_path:
                return True
        # Check is_http_allowed
        if self.is_http_allowed is not None:
            if existing.get('isHttpAllowed') != self.is_http_allowed:
                return True
        # Check is_https_allowed
        if self.is_https_allowed is not None:
            if existing.get('isHttpsAllowed') != self.is_https_allowed:
                return True
        # Check is_compression_enabled
        if self.is_compression_enabled is not None:
            if existing.get('isCompressionEnabled') != self.is_compression_enabled:
                return True
        # Check content_types_to_compress
        if self.content_types_to_compress is not None:
            if existing.get('contentTypesToCompress') != self.content_types_to_compress:
                return True
        # Check query_string_caching_behavior
        if self.query_string_caching_behavior is not None:
            if existing.get('queryStringCachingBehavior') != self.query_string_caching_behavior:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2024-02-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2024-02-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2024-02-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Cdn/profiles/endpoints/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['profile_name'] = response.get('ProfileName')
        _body = response
        _body = _body.get('origins', {}) if isinstance(_body, dict) else {}
        result['origin_host_name'] = _body.get('hostName')
        result['origin_path'] = response.get('originPath')
        result['is_http_allowed'] = response.get('isHttpAllowed')
        result['is_https_allowed'] = response.get('isHttpsAllowed')
        result['is_compression_enabled'] = response.get('isCompressionEnabled')
        result['content_types_to_compress'] = response.get('contentTypesToCompress')
        result['query_string_caching_behavior'] = response.get('queryStringCachingBehavior')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMCdnEndpoint()


if __name__ == '__main__':
    main()
