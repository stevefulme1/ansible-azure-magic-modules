#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_functionapp
version_added: "0.1.0"
short_description: Manage Azure Function App instances.
description:
    - Create, update, or delete an Azure FunctionApp.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the functionapp.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    server_farm_id:
        description:
            - Resource ID of the App Service Plan.
        type: str
    kind:
        description:
            - Kind of the Function App.
        type: str
        default: "functionapp"
    storage_account_name:
        description:
            - Name of the storage account for the Function App.
        type: str
    storage_account_access_key:
        description:
            - Access key for the storage account.
        type: str
    runtime_stack:
        description:
            - Runtime stack for the Function App.
        type: str
        choices:
            - "dotnet"
            - "node"
            - "python"
            - "java"
            - "powershell"
            - "custom"
    runtime_version:
        description:
            - Runtime version for the Function App.
        type: str
    functions_extension_version:
        description:
            - Functions runtime extension version.
        type: str
        default: "~4"
    https_only:
        description:
            - Whether the Function App only accepts HTTPS traffic.
        type: bool
        default: "True"
    state:
        description:
            - Assert the state of the functionapp.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure FunctionApp REST API
      link: https://learn.microsoft.com/en-us/azure/azure-functions/functions-overview
'''

EXAMPLES = r'''
- name: Create FunctionApp
  azure_rm_functionapp:
    resource_group: myResourceGroup
    name: myFunctionApp
    location: eastus
    state: present

- name: Delete FunctionApp
  azure_rm_functionapp:
    resource_group: myResourceGroup
    name: myFunctionApp
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the functionapp.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        server_farm_id:
            description: Resource ID of the App Service Plan.
            type: str
        kind:
            description: Kind of the Function App.
            type: str
        storage_account_name:
            description: Name of the storage account for the Function App.
            type: str
        storage_account_access_key:
            description: Access key for the storage account.
            type: str
        runtime_stack:
            description: Runtime stack for the Function App.
            type: str
        runtime_version:
            description: Runtime version for the Function App.
            type: str
        functions_extension_version:
            description: Functions runtime extension version.
            type: str
        https_only:
            description: Whether the Function App only accepts HTTPS traffic.
            type: bool
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMFunctionApp(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            server_farm_id=dict(
                type='str',
            ),
            kind=dict(
                type='str',
                default='functionapp',
            ),
            storage_account_name=dict(
                type='str',
            ),
            storage_account_access_key=dict(
                type='str',
            ),
            runtime_stack=dict(
                type='str',
                choices=['dotnet', 'node', 'python', 'java', 'powershell', 'custom'],
            ),
            runtime_version=dict(
                type='str',
            ),
            functions_extension_version=dict(
                type='str',
                default='~4',
            ),
            https_only=dict(
                type='bool',
                default=True,
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.server_farm_id = None
        self.kind = None
        self.storage_account_name = None
        self.storage_account_access_key = None
        self.runtime_stack = None
        self.runtime_version = None
        self.functions_extension_version = None
        self.https_only = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.server_farm_id is not None:
            body['serverFarmId'] = self.server_farm_id
        if self.kind is not None:
            body['kind'] = self.kind
        if self.storage_account_name is not None:
            _target = body
            _target = _target.setdefault('siteConfig', {})
            _target = _target.setdefault('appSettings', {})
            _target['AzureWebJobsStorage'] = self.storage_account_name
        if self.storage_account_access_key is not None:
            _target = body
            _target = _target.setdefault('siteConfig', {})
            _target = _target.setdefault('appSettings', {})
            _target['AzureWebJobsStorageKey'] = self.storage_account_access_key
        if self.runtime_stack is not None:
            _target = body
            _target = _target.setdefault('siteConfig', {})
            _target = _target.setdefault('appSettings', {})
            _target['FUNCTIONS_WORKER_RUNTIME'] = self.runtime_stack
        if self.runtime_version is not None:
            _target = body
            _target = _target.setdefault('siteConfig', {})
            _target['linuxFxVersion'] = self.runtime_version
        if self.functions_extension_version is not None:
            _target = body
            _target = _target.setdefault('siteConfig', {})
            _target = _target.setdefault('appSettings', {})
            _target['FUNCTIONS_EXTENSION_VERSION'] = self.functions_extension_version
        if self.https_only is not None:
            body['httpsOnly'] = self.https_only

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check server_farm_id
        if self.server_farm_id is not None:
            if existing.get('serverFarmId') != self.server_farm_id:
                return True
        # Check kind
        if self.kind is not None:
            if existing.get('kind') != self.kind:
                return True
        # Check storage_account_name
        if self.storage_account_name is not None:
            _existing = existing
            _existing = _existing.get('siteConfig', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('appSettings', {}) if isinstance(_existing, dict) else {}
            if _existing.get('AzureWebJobsStorage') != self.storage_account_name:
                return True
        # Check storage_account_access_key
        if self.storage_account_access_key is not None:
            _existing = existing
            _existing = _existing.get('siteConfig', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('appSettings', {}) if isinstance(_existing, dict) else {}
            if _existing.get('AzureWebJobsStorageKey') != self.storage_account_access_key:
                return True
        # Check runtime_stack
        if self.runtime_stack is not None:
            _existing = existing
            _existing = _existing.get('siteConfig', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('appSettings', {}) if isinstance(_existing, dict) else {}
            if _existing.get('FUNCTIONS_WORKER_RUNTIME') != self.runtime_stack:
                return True
        # Check runtime_version
        if self.runtime_version is not None:
            _existing = existing
            _existing = _existing.get('siteConfig', {}) if isinstance(_existing, dict) else {}
            if _existing.get('linuxFxVersion') != self.runtime_version:
                return True
        # Check functions_extension_version
        if self.functions_extension_version is not None:
            _existing = existing
            _existing = _existing.get('siteConfig', {}) if isinstance(_existing, dict) else {}
            _existing = _existing.get('appSettings', {}) if isinstance(_existing, dict) else {}
            if _existing.get('FUNCTIONS_EXTENSION_VERSION') != self.functions_extension_version:
                return True
        # Check https_only
        if self.https_only is not None:
            if existing.get('httpsOnly') != self.https_only:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2023-12-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2023-12-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2023-12-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Web/sites/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['server_farm_id'] = response.get('serverFarmId')
        result['kind'] = response.get('kind')
        _body = response
        _body = _body.get('siteConfig', {}) if isinstance(_body, dict) else {}
        _body = _body.get('appSettings', {}) if isinstance(_body, dict) else {}
        result['storage_account_name'] = _body.get('AzureWebJobsStorage')
        _body = response
        _body = _body.get('siteConfig', {}) if isinstance(_body, dict) else {}
        _body = _body.get('appSettings', {}) if isinstance(_body, dict) else {}
        result['storage_account_access_key'] = _body.get('AzureWebJobsStorageKey')
        _body = response
        _body = _body.get('siteConfig', {}) if isinstance(_body, dict) else {}
        _body = _body.get('appSettings', {}) if isinstance(_body, dict) else {}
        result['runtime_stack'] = _body.get('FUNCTIONS_WORKER_RUNTIME')
        _body = response
        _body = _body.get('siteConfig', {}) if isinstance(_body, dict) else {}
        result['runtime_version'] = _body.get('linuxFxVersion')
        _body = response
        _body = _body.get('siteConfig', {}) if isinstance(_body, dict) else {}
        _body = _body.get('appSettings', {}) if isinstance(_body, dict) else {}
        result['functions_extension_version'] = _body.get('FUNCTIONS_EXTENSION_VERSION')
        result['https_only'] = response.get('httpsOnly')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMFunctionApp()


if __name__ == '__main__':
    main()
