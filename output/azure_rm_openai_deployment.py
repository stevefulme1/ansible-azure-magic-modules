#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_openai_deployment
version_added: "0.1.0"
short_description: Manage Azure OpenAI model deployments.
description:
    - Create, update, or delete an Azure OpenAIDeployment.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the openaideployment.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    account_name:
        description:
            - Name of the Cognitive Services account.
        type: str
        required: true
    model_name:
        description:
            - Name of the model to deploy.
        type: str
        required: true
    model_version:
        description:
            - Version of the model to deploy.
        type: str
        required: true
    model_format:
        description:
            - Format of the model.
        type: str
        default: "OpenAI"
    sku_name:
        description:
            - SKU name for the deployment.
        type: str
        default: "Standard"
    sku_capacity:
        description:
            - SKU capacity in thousands of tokens per minute.
        type: int
    rai_policy_name:
        description:
            - Name of the RAI policy to apply.
        type: str
    state:
        description:
            - Assert the state of the openaideployment.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure OpenAIDeployment REST API
      link: https://learn.microsoft.com/en-us/azure/ai-services/openai/
'''

EXAMPLES = r'''
- name: Create OpenAIDeployment
  azure_rm_openai_deployment:
    resource_group: myResourceGroup
    name: myOpenAIDeployment
    location: eastus
    account_name: "example_value"
    model_name: "example_value"
    model_version: "example_value"
    state: present

- name: Delete OpenAIDeployment
  azure_rm_openai_deployment:
    resource_group: myResourceGroup
    name: myOpenAIDeployment
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the openaideployment.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        account_name:
            description: Name of the Cognitive Services account.
            type: str
        model_name:
            description: Name of the model to deploy.
            type: str
        model_version:
            description: Version of the model to deploy.
            type: str
        model_format:
            description: Format of the model.
            type: str
        sku_name:
            description: SKU name for the deployment.
            type: str
        sku_capacity:
            description: SKU capacity in thousands of tokens per minute.
            type: int
        rai_policy_name:
            description: Name of the RAI policy to apply.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMOpenAIDeployment(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            account_name=dict(
                type='str',
                required=True,
            ),
            model_name=dict(
                type='str',
                required=True,
            ),
            model_version=dict(
                type='str',
                required=True,
            ),
            model_format=dict(
                type='str',
                default='OpenAI',
            ),
            sku_name=dict(
                type='str',
                default='Standard',
            ),
            sku_capacity=dict(
                type='int',
            ),
            rai_policy_name=dict(
                type='str',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.account_name = None
        self.model_name = None
        self.model_version = None
        self.model_format = None
        self.sku_name = None
        self.sku_capacity = None
        self.rai_policy_name = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.account_name is not None:
            body['AccountName'] = self.account_name
        if self.sku_name is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['name'] = self.sku_name
        if self.sku_capacity is not None:
            _target = body
            _target = _target.setdefault('sku', {})
            _target['capacity'] = self.sku_capacity

        properties = body.setdefault('properties', {})
        if self.model_name is not None:
            _target = properties
            _target = _target.setdefault('model', {})
            _target['name'] = self.model_name
        if self.model_version is not None:
            _target = properties
            _target = _target.setdefault('model', {})
            _target['version'] = self.model_version
        if self.model_format is not None:
            _target = properties
            _target = _target.setdefault('model', {})
            _target['format'] = self.model_format
        if self.rai_policy_name is not None:
            properties['raiPolicyName'] = self.rai_policy_name

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check account_name
        if self.account_name is not None:
            if existing.get('AccountName') != self.account_name:
                return True
        # Check model_name
        if self.model_name is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('model', {}) if isinstance(_existing, dict) else {}
            if _existing.get('name') != self.model_name:
                return True
        # Check model_version
        if self.model_version is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('model', {}) if isinstance(_existing, dict) else {}
            if _existing.get('version') != self.model_version:
                return True
        # Check model_format
        if self.model_format is not None:
            _existing = existing.get('properties', {})
            _existing = _existing.get('model', {}) if isinstance(_existing, dict) else {}
            if _existing.get('format') != self.model_format:
                return True
        # Check sku_name
        if self.sku_name is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('name') != self.sku_name:
                return True
        # Check sku_capacity
        if self.sku_capacity is not None:
            _existing = existing
            _existing = _existing.get('sku', {}) if isinstance(_existing, dict) else {}
            if _existing.get('capacity') != self.sku_capacity:
                return True
        # Check rai_policy_name
        if self.rai_policy_name is not None:
            if existing.get('properties', {}).get('raiPolicyName') != self.rai_policy_name:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2023-05-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2023-05-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2023-05-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.CognitiveServices/accounts/deployments/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['account_name'] = response.get('AccountName')
        _props = response.get('properties', {})
        _props = _props.get('model', {}) if isinstance(_props, dict) else {}
        result['model_name'] = _props.get('name')
        _props = response.get('properties', {})
        _props = _props.get('model', {}) if isinstance(_props, dict) else {}
        result['model_version'] = _props.get('version')
        _props = response.get('properties', {})
        _props = _props.get('model', {}) if isinstance(_props, dict) else {}
        result['model_format'] = _props.get('format')
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_name'] = _body.get('name')
        _body = response
        _body = _body.get('sku', {}) if isinstance(_body, dict) else {}
        result['sku_capacity'] = _body.get('capacity')
        result['rai_policy_name'] = response.get('properties', {}).get('raiPolicyName')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMOpenAIDeployment()


if __name__ == '__main__':
    main()
