#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_networkinterface
version_added: "0.1.0"
short_description: Manage Azure network interfaces.
description:
    - Create, update, or delete an Azure NetworkInterface.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the networkinterface.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    ip_configurations:
        description:
            - List of IP configurations for the network interface.
        type: list
    network_security_group_id:
        description:
            - Resource ID of the network security group.
        type: str
    enable_accelerated_networking:
        description:
            - Enable accelerated networking on the interface.
        type: bool
    enable_ip_forwarding:
        description:
            - Enable IP forwarding on the interface.
        type: bool
    state:
        description:
            - Assert the state of the networkinterface.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure NetworkInterface REST API
      link: https://learn.microsoft.com/en-us/azure/virtual-network/virtual-network-network-interface
'''

EXAMPLES = r'''
- name: Create NetworkInterface
  azure_rm_networkinterface:
    resource_group: myResourceGroup
    name: myNetworkInterface
    location: eastus
    state: present

- name: Delete NetworkInterface
  azure_rm_networkinterface:
    resource_group: myResourceGroup
    name: myNetworkInterface
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the networkinterface.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        ip_configurations:
            description: List of IP configurations for the network interface.
            type: list
        network_security_group_id:
            description: Resource ID of the network security group.
            type: str
        enable_accelerated_networking:
            description: Enable accelerated networking on the interface.
            type: bool
        enable_ip_forwarding:
            description: Enable IP forwarding on the interface.
            type: bool
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMNetworkInterface(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            ip_configurations=dict(
                type='list',
            ),
            network_security_group_id=dict(
                type='str',
            ),
            enable_accelerated_networking=dict(
                type='bool',
            ),
            enable_ip_forwarding=dict(
                type='bool',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.ip_configurations = None
        self.network_security_group_id = None
        self.enable_accelerated_networking = None
        self.enable_ip_forwarding = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.ip_configurations is not None:
            body['ipConfigurations'] = self.ip_configurations
        if self.network_security_group_id is not None:
            _target = body
            _target = _target.setdefault('networkSecurityGroup', {})
            _target['id'] = self.network_security_group_id
        if self.enable_accelerated_networking is not None:
            body['enableAcceleratedNetworking'] = self.enable_accelerated_networking
        if self.enable_ip_forwarding is not None:
            body['enableIPForwarding'] = self.enable_ip_forwarding

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check ip_configurations
        if self.ip_configurations is not None:
            if existing.get('ipConfigurations') != self.ip_configurations:
                return True
        # Check network_security_group_id
        if self.network_security_group_id is not None:
            _existing = existing
            _existing = _existing.get('networkSecurityGroup', {}) if isinstance(_existing, dict) else {}
            if _existing.get('id') != self.network_security_group_id:
                return True
        # Check enable_accelerated_networking
        if self.enable_accelerated_networking is not None:
            if existing.get('enableAcceleratedNetworking') != self.enable_accelerated_networking:
                return True
        # Check enable_ip_forwarding
        if self.enable_ip_forwarding is not None:
            if existing.get('enableIPForwarding') != self.enable_ip_forwarding:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2024-03-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2024-03-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2024-03-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Network/networkInterfaces/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['ip_configurations'] = response.get('ipConfigurations')
        _body = response
        _body = _body.get('networkSecurityGroup', {}) if isinstance(_body, dict) else {}
        result['network_security_group_id'] = _body.get('id')
        result['enable_accelerated_networking'] = response.get('enableAcceleratedNetworking')
        result['enable_ip_forwarding'] = response.get('enableIPForwarding')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMNetworkInterface()


if __name__ == '__main__':
    main()
