#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright: (c) 2024, Azure Magic Modules Generator
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_managedapplication
version_added: "0.1.0"
short_description: Manage Azure Managed Application instances.
description:
    - Create, update, or delete an Azure ManagedApplication.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the managedapplication.
        type: str
        required: true
    location:
        description:
            - Valid Azure location. Defaults to the location of the resource group.
        type: str
    kind:
        description:
            - Kind of managed application.
        type: str
        choices:
            - "MarketPlace"
            - "ServiceCatalog"
    managed_resource_group_id:
        description:
            - Resource ID of the managed resource group.
        type: str
    application_definition_id:
        description:
            - Resource ID of the application definition.
        type: str
    parameters:
        description:
            - Application parameters.
        type: dict
    plan_name:
        description:
            - Name of the plan.
        type: str
    plan_publisher:
        description:
            - Publisher of the plan.
        type: str
    plan_product:
        description:
            - Product of the plan.
        type: str
    plan_version:
        description:
            - Version of the plan.
        type: str
    state:
        description:
            - Assert the state of the managedapplication.
            - Use C(present) to create or update and C(absent) to delete.
        type: str
        default: present
        choices:
            - present
            - absent
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Azure Magic Modules Generator (@azure-magic-modules)"
seealso:
    - description: Azure ManagedApplication REST API
      link: https://learn.microsoft.com/en-us/azure/azure-resource-manager/managed-applications/overview
'''

EXAMPLES = r'''
- name: Create ManagedApplication
  azure_rm_managedapplication:
    resource_group: myResourceGroup
    name: myManagedApplication
    location: eastus
    state: present

- name: Delete ManagedApplication
  azure_rm_managedapplication:
    resource_group: myResourceGroup
    name: myManagedApplication
    state: absent
'''

RETURN = r'''
state:
    description: Current state of the managedapplication.
    returned: always
    type: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        kind:
            description: Kind of managed application.
            type: str
        managed_resource_group_id:
            description: Resource ID of the managed resource group.
            type: str
        application_definition_id:
            description: Resource ID of the application definition.
            type: str
        parameters:
            description: Application parameters.
            type: dict
        plan_name:
            description: Name of the plan.
            type: str
        plan_publisher:
            description: Publisher of the plan.
            type: str
        plan_product:
            description: Product of the plan.
            type: str
        plan_version:
            description: Version of the plan.
            type: str
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMManagedApplication(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str', required=True),
            location=dict(type='str'),
            kind=dict(
                type='str',
                choices=['MarketPlace', 'ServiceCatalog'],
            ),
            managed_resource_group_id=dict(
                type='str',
            ),
            application_definition_id=dict(
                type='str',
            ),
            parameters=dict(
                type='dict',
            ),
            plan_name=dict(
                type='str',
            ),
            plan_publisher=dict(
                type='str',
            ),
            plan_product=dict(
                type='str',
            ),
            plan_version=dict(
                type='str',
            ),
            state=dict(type='str', default='present', choices=['present', 'absent']),
        )

        self.resource_group = None
        self.name = None
        self.location = None
        self.kind = None
        self.managed_resource_group_id = None
        self.application_definition_id = None
        self.parameters = None
        self.plan_name = None
        self.plan_publisher = None
        self.plan_product = None
        self.plan_version = None
        self.state = None
        self.tags = None

        self.results = dict(changed=False, state=dict())

        required_if = [
            ('state', 'present', ['resource_group', 'name']),
        ]

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=True,
            required_if=required_if,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        resource_group = self.resource_group
        name = self.name

        # Get existing resource
        response = self.get_resource()

        if self.state == 'present':
            if not self.location:
                rg = self.get_resource_group(resource_group)
                self.location = rg.location

            # Build the resource body from parameters
            body = self.build_body()

            if response:
                # Check if update needed
                update_tags, new_tags = self.update_tags(response.get('tags'))
                if update_tags:
                    body['tags'] = new_tags
                    self.results['changed'] = True

                if self.check_update(response, body):
                    self.results['changed'] = True

                if self.results['changed']:
                    if not self.check_mode:
                        response = self.create_or_update(resource_group, name, body)
                else:
                    response = response
            else:
                if self.tags:
                    body['tags'] = self.tags
                if not self.check_mode:
                    response = self.create_or_update(resource_group, name, body)
                self.results['changed'] = True

        elif self.state == 'absent':
            if response:
                if not self.check_mode:
                    self.delete_resource(resource_group, name)
                self.results['changed'] = True

        if response:
            self.results['state'] = self.format_response(response)

        return self.results

    def build_body(self):
        body = dict(
            location=self.location,
        )
        if self.kind is not None:
            body['kind'] = self.kind
        if self.managed_resource_group_id is not None:
            body['managedResourceGroupId'] = self.managed_resource_group_id
        if self.application_definition_id is not None:
            body['applicationDefinitionId'] = self.application_definition_id
        if self.parameters is not None:
            body['parameters'] = self.parameters
        if self.plan_name is not None:
            _target = body
            _target = _target.setdefault('plan', {})
            _target['name'] = self.plan_name
        if self.plan_publisher is not None:
            _target = body
            _target = _target.setdefault('plan', {})
            _target['publisher'] = self.plan_publisher
        if self.plan_product is not None:
            _target = body
            _target = _target.setdefault('plan', {})
            _target['product'] = self.plan_product
        if self.plan_version is not None:
            _target = body
            _target = _target.setdefault('plan', {})
            _target['version'] = self.plan_version

        if self.tags:
            body['tags'] = self.tags

        return body

    def check_update(self, existing, desired):
        """Compare existing resource with desired state."""
        # Check kind
        if self.kind is not None:
            if existing.get('kind') != self.kind:
                return True
        # Check managed_resource_group_id
        if self.managed_resource_group_id is not None:
            if existing.get('managedResourceGroupId') != self.managed_resource_group_id:
                return True
        # Check application_definition_id
        if self.application_definition_id is not None:
            if existing.get('applicationDefinitionId') != self.application_definition_id:
                return True
        # Check parameters
        if self.parameters is not None:
            if existing.get('parameters') != self.parameters:
                return True
        # Check plan_name
        if self.plan_name is not None:
            _existing = existing
            _existing = _existing.get('plan', {}) if isinstance(_existing, dict) else {}
            if _existing.get('name') != self.plan_name:
                return True
        # Check plan_publisher
        if self.plan_publisher is not None:
            _existing = existing
            _existing = _existing.get('plan', {}) if isinstance(_existing, dict) else {}
            if _existing.get('publisher') != self.plan_publisher:
                return True
        # Check plan_product
        if self.plan_product is not None:
            _existing = existing
            _existing = _existing.get('plan', {}) if isinstance(_existing, dict) else {}
            if _existing.get('product') != self.plan_product:
                return True
        # Check plan_version
        if self.plan_version is not None:
            _existing = existing
            _existing = _existing.get('plan', {}) if isinstance(_existing, dict) else {}
            if _existing.get('version') != self.plan_version:
                return True

        return False

    def get_resource(self):
        try:
            url = self.get_resource_url()
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2021-07-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def create_or_update(self, resource_group, name, body):
        url = self.get_resource_url()
        response = self.mgmt_client.query(
            url, "PUT",
            {'api-version': '2021-07-01'},
            None, body, [200, 201], 0, 0,
        )
        return self.deserialize_response(response)

    def delete_resource(self, resource_group, name):
        url = self.get_resource_url()
        self.mgmt_client.query(
            url, "DELETE",
            {'api-version': '2021-07-01'},
            None, None, [200, 202, 204], 0, 0,
        )

    def get_resource_url(self):
        return (
            '/subscriptions/{subscription_id}'
            '/resourceGroups/{resource_group}'
            '/providers/Microsoft.Solutions/applications/{name}'
        ).format(
            subscription_id=self.subscription_id,
            resource_group=self.resource_group,
            name=self.name,
        )

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['kind'] = response.get('kind')
        result['managed_resource_group_id'] = response.get('managedResourceGroupId')
        result['application_definition_id'] = response.get('applicationDefinitionId')
        result['parameters'] = response.get('parameters')
        _body = response
        _body = _body.get('plan', {}) if isinstance(_body, dict) else {}
        result['plan_name'] = _body.get('name')
        _body = response
        _body = _body.get('plan', {}) if isinstance(_body, dict) else {}
        result['plan_publisher'] = _body.get('publisher')
        _body = response
        _body = _body.get('plan', {}) if isinstance(_body, dict) else {}
        result['plan_product'] = _body.get('product')
        _body = response
        _body = _body.get('plan', {}) if isinstance(_body, dict) else {}
        result['plan_version'] = _body.get('version')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMManagedApplication()


if __name__ == '__main__':
    main()
