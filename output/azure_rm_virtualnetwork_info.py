#!/usr/bin/python
# -*- coding: utf-8 -*-
# Auto-generated by Azure Magic Modules — do not edit manually

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: azure_rm_virtualnetwork_info
version_added: "0.1.0"
short_description: Get facts for Azure VirtualNetwork
description:
    - Get facts for a specific VirtualNetwork or list all VirtualNetworks in a resource group.
    - This module was auto-generated by Azure Magic Modules.
options:
    resource_group:
        description:
            - Name of the resource group.
        type: str
        required: true
    name:
        description:
            - Name of the virtualnetwork.
            - If not specified, all virtualnetworks in the resource group will be listed.
        type: str
extends_documentation_fragment:
    - azure.azcollection.azure
    - azure.azcollection.azure_tags
author:
    - "Ansible Team"
seealso:
    - name: Azure VirtualNetwork REST API
      link: https://learn.microsoft.com/en-us/azure/virtual-network/
'''

EXAMPLES = r'''
- name: Get facts for a specific VirtualNetwork
  azure_rm_virtualnetwork_info:
    resource_group: myResourceGroup
    name: myVirtualNetwork

- name: List all VirtualNetworks in a resource group
  azure_rm_virtualnetwork_info:
    resource_group: myResourceGroup
'''

RETURN = r'''
virtualnetworks:
    description:
        - List of virtualnetwork dicts.
    returned: always
    type: list
    elements: dict
    contains:
        id:
            description: Resource ID.
            type: str
        name:
            description: Resource name.
            type: str
        location:
            description: Resource location.
            type: str
        tags:
            description: Resource tags.
            type: dict
        address_prefixes:
            description: List of address prefixes for the virtual network.
            type: list
        dns_servers:
            description: List of DNS server IP addresses.
            type: list
        enable_ddos_protection:
            description: Enable DDoS protection for the virtual network.
            type: bool
        bgp_communities:
            description: BGP communities associated with the virtual network.
            type: dict
'''

import json
from ansible_collections.azure.azcollection.plugins.module_utils.azure_rm_common import AzureRMModuleBase


class AzureRMVirtualNetworkInfo(AzureRMModuleBase):

    def __init__(self):
        self.module_arg_spec = dict(
            resource_group=dict(type='str', required=True, aliases=['resource_group_name']),
            name=dict(type='str'),
        )

        self.resource_group = None
        self.name = None
        self.tags = None

        self.results = dict(
            changed=False,
            virtualnetworks=[],
        )

        super().__init__(
            derived_arg_spec=self.module_arg_spec,
            supports_check_mode=True,
            supports_tags=False,
            facts_module=True,
        )

    def exec_module(self, **kwargs):
        for key in list(self.module_arg_spec.keys()) + ['tags']:
            setattr(self, key, kwargs[key])

        result_key = 'virtualnetworks'

        if self.name:
            response = self.get_resource()
            if response:
                if self.has_tags(response.get('tags'), self.tags):
                    self.results[result_key] = [self.format_response(response)]
        else:
            responses = self.list_by_resource_group()
            for item in responses:
                if self.has_tags(item.get('tags'), self.tags):
                    self.results[result_key].append(self.format_response(item))

        return self.results

    def get_resource(self):
        try:
            url = (
                '/subscriptions/{subscription_id}'
                '/resourceGroups/{resource_group}'
                '/providers/Microsoft.Network/virtualNetworks/{name}'
            ).format(
                subscription_id=self.subscription_id,
                resource_group=self.resource_group,
                name=self.name,
            )
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2023-09-01'},
                None, None, [200], 0, 0,
            )
            return self.deserialize_response(response)
        except Exception:
            return None

    def list_by_resource_group(self):
        try:
            url = (
                '/subscriptions/{subscription_id}'
                '/resourceGroups/{resource_group}'
                '/providers/Microsoft.Network/virtualNetworks'
            ).format(
                subscription_id=self.subscription_id,
                resource_group=self.resource_group,
            )
            response = self.mgmt_client.query(
                url, "GET",
                {'api-version': '2023-09-01'},
                None, None, [200], 0, 0,
            )
            result = self.deserialize_response(response)
            if result and isinstance(result, dict):
                return result.get('value', [])
            return []
        except Exception:
            return []

    def format_response(self, response):
        result = dict(
            id=response.get('id'),
            name=response.get('name'),
            location=response.get('location'),
            tags=response.get('tags'),
        )
        result['address_prefixes'] = response.get('properties', {}).get('AddressPrefixes')
        result['dns_servers'] = response.get('properties', {}).get('DnsServers')
        result['enable_ddos_protection'] = response.get('properties', {}).get('EnableDdosProtection')
        result['bgp_communities'] = response.get('properties', {}).get('BgpCommunities')
        return result

    @staticmethod
    def deserialize_response(response):
        try:
            return json.loads(response.text)
        except (ValueError, AttributeError):
            return None


def main():
    AzureRMVirtualNetworkInfo()


if __name__ == '__main__':
    main()
